---
phase: 01-security-auth-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/login/page.tsx
  - app/login/layout.tsx
  - app/page.tsx
  - middleware.ts
  - lib/supabase/client.ts
  - lib/supabase/middleware.ts
  - lib/auth/session-hydrator.ts
  - store/sessionStore.ts
  - components/auth/AuthProvider.tsx
  - components/auth/SessionTimeoutModal.tsx
  - components/auth/LogoutButton.tsx
  - app/(tenant)/[tenantId]/layout.tsx
  - components/Sidebar.tsx
autonomous: false
requirements:
  - SEC-04
  - SEC-05
  - SEC-06
  - SEC-07
  - SEC-08

must_haves:
  truths:
    - "User can log in with email/password on /login page and is redirected to their tenant dashboard"
    - "User who is not logged in is redirected to /login when visiting any tenant route"
    - "User can log out and all ePHI is cleared from localStorage while theme/accent preferences are preserved"
    - "User session persists across browser refresh (page reload does not log user out)"
    - "sessionStore contains real user data from Supabase JWT claims (not mock data)"
    - "After 28 minutes of inactivity a warning modal appears with 'Stay logged in' button"
    - "After 30 minutes of inactivity user is automatically logged out"
  artifacts:
    - path: "app/login/page.tsx"
      provides: "Login page with glassmorphism design"
      min_lines: 50
    - path: "middleware.ts"
      provides: "Route protection middleware"
      contains: "getUser"
    - path: "lib/supabase/client.ts"
      provides: "Browser Supabase client"
      exports: ["createClient"]
    - path: "lib/supabase/middleware.ts"
      provides: "Middleware Supabase client helper"
      exports: ["updateSession"]
    - path: "lib/auth/session-hydrator.ts"
      provides: "Supabase Session to AppSession mapper"
      exports: ["hydrateFromSupabaseSession"]
    - path: "store/sessionStore.ts"
      provides: "Session store with null initial state and real hydration"
    - path: "components/auth/AuthProvider.tsx"
      provides: "Auth state listener component"
    - path: "components/auth/SessionTimeoutModal.tsx"
      provides: "Inactivity warning modal with countdown"
    - path: "components/auth/LogoutButton.tsx"
      provides: "Logout with ePHI cleanup"
  key_links:
    - from: "middleware.ts"
      to: "lib/supabase/middleware.ts"
      via: "updateSession helper"
      pattern: "updateSession"
    - from: "components/auth/AuthProvider.tsx"
      to: "lib/auth/session-hydrator.ts"
      via: "hydrateFromSupabaseSession on auth state change"
      pattern: "hydrateFromSupabaseSession"
    - from: "components/auth/AuthProvider.tsx"
      to: "store/sessionStore.ts"
      via: "setSession/clearSession on auth events"
      pattern: "setSession|clearSession"
    - from: "app/login/page.tsx"
      to: "lib/supabase/client.ts"
      via: "signInWithPassword"
      pattern: "signInWithPassword"
    - from: "components/auth/LogoutButton.tsx"
      to: "lib/supabase/client.ts"
      via: "signOut + ePHI cleanup"
      pattern: "signOut"
---

<objective>
Implement end-to-end Supabase Auth: login page with glassmorphism, route protection middleware, session hydration from real JWT claims, logout with ePHI cleanup, and 30-minute inactivity timeout with warning modal.

Purpose: This is the auth experience clinicians will use every day. After this plan, the app has real authentication -- no more mock sessions. Users log in with Supabase credentials, sessions persist via cookies, protected routes redirect to /login, and logout clears all clinical data from the browser.

Output: Complete auth flow from login to logout with session management, route protection, and HIPAA-compliant inactivity timeout.
</objective>

<execution_context>
@C:/Users/duytr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/duytr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-auth-foundation/01-CONTEXT.md
@.planning/phases/01-security-auth-foundation/01-RESEARCH.md
@.planning/phases/01-security-auth-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Types the executor needs -->

From types/session.ts:
```typescript
export type StaffRole = "doctor" | "technician" | "receptionist" | "admin" | "owner"
export type EntitlementKey = "scheduling" | "patient_demographics" | "basic_exam" | ...
export interface AppSession {
  user: UserSession;
  tenant: TenantSession;
  accessToken: string;
  expiresAt: Date;
}
export interface UserSession {
  userId: string; staffId: string; email: string; fullName: string;
  role: StaffRole; clinicalRole?: StaffRole; isSuperuser: boolean; avatarInitials: string;
}
export interface TenantSession {
  tenantId: string; schemaName: string; clinicName: string;
  planName: PlanName; entitlements: Set<EntitlementKey>;
}
```

From store/sessionStore.ts (after Plan 01 devtools fix):
```typescript
interface SessionState {
  session: AppSession | null;
  isLoading: boolean;
  setSession: (session: AppSession) => void;
  clearSession: () => void;
  isExpired: () => boolean;
}
// Currently seeded with getMockSession("premium_doctor") -- must become null
```

Existing glass styling classes (from globals.css):
- .glass-card -- backdrop-blur, semi-transparent bg, border
- .glass-card-hover -- hover scale + shadow
- .glass-input -- glass-styled input field
- .animate-enter -- fade-in-up animation

From lib/supabase/server.ts (created in Plan 02):
```typescript
export async function createServerSupabaseClient(): Promise<SupabaseClient>
```

Zustand stores with clinical data to clear on logout:
- encounterStore, vitalsStore, refractionStore, examFindingsStore, diagnosisStore, problemListStore
Stores to KEEP on logout (non-PHI):
- themeStore (dark/light preference), tenantCustomizationStore (accent color, logo)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Supabase client factories, middleware, session hydrator, and login page</name>
  <files>
    lib/supabase/client.ts
    lib/supabase/middleware.ts
    lib/auth/session-hydrator.ts
    middleware.ts
    app/login/layout.tsx
    app/login/page.tsx
    app/page.tsx
  </files>
  <action>
    1. Install react-idle-timer: `npm install react-idle-timer`

    2. Create lib/supabase/client.ts -- browser-side Supabase client:
       - Import createBrowserClient from '@supabase/ssr'
       - Export function createClient() that returns createBrowserClient(NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY)
       - This is used in client components (login form, auth provider, logout)

    3. Create lib/supabase/middleware.ts -- middleware helper:
       - Import createServerClient from '@supabase/ssr'
       - Import NextResponse, NextRequest from 'next/server'
       - Export async function updateSession(request: NextRequest) that:
         a. Creates supabaseResponse = NextResponse.next({ request })
         b. Creates a server client with cookie read/write handlers that sync cookies between request and response
         c. Calls supabase.auth.getUser() (NOT getSession -- must revalidate server-side)
         d. If no user AND path matches a tenant route (regex: /^\/[^/]+\// but NOT /login, /api, /_next, /favicon):
            - Build login URL with returnTo search param = current pathname
            - Return NextResponse.redirect(loginUrl)
         e. If user exists AND path is /login:
            - Extract tenant_id from user.app_metadata, fallback to 'demo-clinic'
            - Redirect to /{tenantId}/dashboard
         f. Return supabaseResponse (with refreshed cookies)

    4. Create middleware.ts at project root (SEC-07):
       - Import updateSession from '@/lib/supabase/middleware'
       - Export async function middleware(request) that calls updateSession(request)
       - Export config.matcher that matches all routes EXCEPT static files, images, and prefetch:
         ```
         matcher: [{
           source: '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
           missing: [
             { type: 'header', key: 'next-router-prefetch' },
             { type: 'header', key: 'purpose', value: 'prefetch' },
           ],
         }]
         ```

    5. Create lib/auth/session-hydrator.ts (SEC-08):
       - Import Session type from '@supabase/supabase-js'
       - Import AppSession, EntitlementKey, StaffRole from '@/types/session'
       - Export function hydrateFromSupabaseSession(session: Session): AppSession
         - Extract user from session.user
         - Extract app_metadata (tenant_id, role, staff_id, schema_name, clinic_name, plan_name, entitlements, clinical_role, is_superuser)
         - Map to AppSession shape following the exact pattern from 01-RESEARCH.md Pattern 3
         - Build avatarInitials from full_name or email (first letter of each word, max 2 chars)
         - accessToken = session.access_token
         - expiresAt = new Date(session.expires_at! * 1000)

    6. Create app/login/layout.tsx:
       - Simple layout that renders children without the tenant shell (Sidebar, TopNav)
       - Full viewport height, ambient gradient background matching the tenant layout aesthetic
       - Import and apply the same gradient classes used in app/(tenant)/[tenantId]/layout.tsx

    7. Create app/login/page.tsx (SEC-04):
       - 'use client' component
       - Centered glass card on ambient gradient background (per user decision: glassmorphism design)
       - Form with email and password fields using glass-input styling
       - "Sign In" button using the existing Button component (primary variant)
       - On submit:
         a. Call createClient().auth.signInWithPassword({ email, password })
         b. On success: router.push(returnTo from searchParams, or fallback to user's tenant dashboard)
         c. On error: Show inline error text below form -- "Invalid email or password" style, red text (text-red-500), accessible (role="alert")
       - Loading state: disable button, show spinner during auth call
       - Read returnTo from URL searchParams to redirect after login
       - App logo/name at top of the card
       - No "Sign up" link (admin-created accounts only, per user decision)

    8. Update app/page.tsx:
       - Change redirect from '/demo-clinic/dashboard' to '/login'
       - This ensures the root URL sends users to login (middleware will redirect authenticated users to their dashboard)
  </action>
  <verify>
    <automated>cd "C:/Users/duytr/OneDrive/Desktop/clarityos-erp" && node -e "
const fs = require('fs');
const files = [
  'lib/supabase/client.ts',
  'lib/supabase/middleware.ts',
  'lib/auth/session-hydrator.ts',
  'middleware.ts',
  'app/login/layout.tsx',
  'app/login/page.tsx',
];
for (const f of files) {
  if (!fs.existsSync(f)) { console.error('MISSING:', f); process.exit(1); }
}
// Check key patterns
const mw = fs.readFileSync('middleware.ts', 'utf8');
if (!mw.includes('updateSession')) { console.error('middleware.ts missing updateSession'); process.exit(1); }
const mwHelper = fs.readFileSync('lib/supabase/middleware.ts', 'utf8');
if (!mwHelper.includes('getUser')) { console.error('middleware helper missing getUser'); process.exit(1); }
if (mwHelper.includes('getSession')) { console.error('middleware helper uses getSession instead of getUser -- SECURITY ISSUE'); process.exit(1); }
const hydrator = fs.readFileSync('lib/auth/session-hydrator.ts', 'utf8');
if (!hydrator.includes('hydrateFromSupabaseSession')) { console.error('hydrator missing export'); process.exit(1); }
const login = fs.readFileSync('app/login/page.tsx', 'utf8');
if (!login.includes('signInWithPassword')) { console.error('login missing signInWithPassword'); process.exit(1); }
const root = fs.readFileSync('app/page.tsx', 'utf8');
if (root.includes('demo-clinic')) { console.error('app/page.tsx still redirects to demo-clinic'); process.exit(1); }
console.log('All auth files present and valid');
" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - lib/supabase/client.ts exports browser client factory
    - lib/supabase/middleware.ts exports updateSession() using getUser() (NOT getSession)
    - middleware.ts at project root protects all tenant routes, redirects to /login with returnTo param
    - lib/auth/session-hydrator.ts maps Supabase Session to AppSession
    - app/login/page.tsx renders glassmorphism login form with email/password, inline error display, returnTo redirect
    - app/page.tsx redirects to /login instead of /demo-clinic/dashboard
    - Next.js build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire session hydration, AuthProvider, logout with ePHI cleanup, and inactivity timeout</name>
  <files>
    store/sessionStore.ts
    components/auth/AuthProvider.tsx
    components/auth/SessionTimeoutModal.tsx
    components/auth/LogoutButton.tsx
    app/(tenant)/[tenantId]/layout.tsx
    components/Sidebar.tsx
  </files>
  <action>
    1. Update store/sessionStore.ts (SEC-08):
       - Remove the import of getMockSession from '@/lib/auth/mock-session'
       - Change initial session from `getMockSession("premium_doctor")` to `null`
       - Change initial isLoading from `false` to `true` (loading until auth state resolves)
       - Keep all existing methods (setSession, clearSession, isExpired) and selectors
       - The devtools conditional from Plan 01 should already be in place; keep it

    2. Create components/auth/AuthProvider.tsx:
       - 'use client' component
       - Props: children: React.ReactNode
       - On mount (useEffect), set up Supabase auth state listener:
         a. Create browser client via createClient()
         b. Call supabase.auth.getSession() to get initial session
         c. If session exists, call useSessionStore.getState().setSession(hydrateFromSupabaseSession(session))
         d. Set isLoading to false
         e. Subscribe to supabase.auth.onAuthStateChange((event, session) => ...)
            - On SIGNED_IN or TOKEN_REFRESHED with session: call setSession(hydrateFromSupabaseSession(session))
            - On SIGNED_OUT: call clearSession()
         f. Return cleanup: subscription.unsubscribe()
       - Render: return children (no wrapper div needed)

    3. Create components/auth/SessionTimeoutModal.tsx (part of SEC-05):
       - 'use client' component
       - Uses react-idle-timer's useIdleTimer hook
       - Two timers:
         a. Warning timer: timeout = 28 minutes (1_680_000ms). onIdle: show warning modal
         b. Logout timer: timeout = 30 minutes (1_800_000ms). onIdle: trigger logout
       - Warning modal UI:
         - Glass card overlay (fixed position, z-50, backdrop-blur)
         - Title: "Session Expiring"
         - Countdown from 120 seconds (2 minutes), updated every second via setInterval
         - "Stay Logged In" primary button that: resets both idle timers (via reset()), hides modal, clears interval
         - Text: "Your session will expire in {seconds} seconds due to inactivity"
         - Per user decision: "feels urgent but not alarming"
       - Logout handler:
         a. Call createClient().auth.signOut()
         b. Call clearEphi() function (see below)
         c. Call useSessionStore.getState().clearSession()
         d. router.push('/login')

    4. Create components/auth/LogoutButton.tsx (SEC-05):
       - 'use client' component
       - Props: className?: string
       - Renders a Button (from @/components/ui/button) with "Log Out" text
       - On click:
         a. Check if any Zustand stores have unsaved/dirty state. If so, show browser confirm("You have unsaved changes. Log out anyway?"). If cancelled, return
         b. Call createClient().auth.signOut()
         c. Clear all ePHI from localStorage:
            - Iterate localStorage keys, remove any matching: draft-transcript-*, encounter-*, clinical-*
            - Reset clinical Zustand stores to initial state: encounterStore, vitalsStore, refractionStore, examFindingsStore, diagnosisStore, problemListStore (call their reset/clear methods or getState() and set empty)
         d. Do NOT clear themeStore or tenantCustomizationStore (non-PHI preferences)
         e. Call useSessionStore.getState().clearSession()
         f. router.push('/login')
       - Export the clearEphi() function separately so SessionTimeoutModal can reuse it

    5. Update app/(tenant)/[tenantId]/layout.tsx:
       - Wrap children with AuthProvider component
       - Add SessionTimeoutModal inside the layout (renders the modal overlay when needed)
       - Keep all existing layout structure (ambient-bg, Sidebar, TopNav)
       - AuthProvider should be the outermost wrapper inside the layout body

    6. Update components/Sidebar.tsx:
       - Add LogoutButton at the bottom of the sidebar navigation
       - Place it below all nav items, at the bottom of the sidebar
       - Use appropriate styling to match the existing sidebar aesthetic
  </action>
  <verify>
    <automated>cd "C:/Users/duytr/OneDrive/Desktop/clarityos-erp" && node -e "
const fs = require('fs');
const files = [
  'components/auth/AuthProvider.tsx',
  'components/auth/SessionTimeoutModal.tsx',
  'components/auth/LogoutButton.tsx',
];
for (const f of files) {
  if (!fs.existsSync(f)) { console.error('MISSING:', f); process.exit(1); }
}
// Verify sessionStore no longer uses mock
const store = fs.readFileSync('store/sessionStore.ts', 'utf8');
if (store.includes('getMockSession')) { console.error('sessionStore still imports getMockSession'); process.exit(1); }
if (store.includes('premium_doctor')) { console.error('sessionStore still seeds mock session'); process.exit(1); }
// Verify AuthProvider is wired in tenant layout
const layout = fs.readFileSync('app/(tenant)/[tenantId]/layout.tsx', 'utf8');
if (!layout.includes('AuthProvider')) { console.error('tenant layout missing AuthProvider'); process.exit(1); }
if (!layout.includes('SessionTimeoutModal')) { console.error('tenant layout missing SessionTimeoutModal'); process.exit(1); }
// Verify LogoutButton clears ePHI
const logout = fs.readFileSync('components/auth/LogoutButton.tsx', 'utf8');
if (!logout.includes('signOut')) { console.error('LogoutButton missing signOut'); process.exit(1); }
if (!logout.includes('localStorage')) { console.error('LogoutButton missing localStorage cleanup'); process.exit(1); }
console.log('All auth wiring verified');
" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - sessionStore starts with null session and isLoading: true (no mock data)
    - AuthProvider listens to Supabase auth state changes and hydrates sessionStore with real JWT claims
    - SessionTimeoutModal warns at 28 minutes, auto-logs out at 30 minutes
    - LogoutButton clears all ePHI (localStorage keys + clinical Zustand stores) but preserves theme/accent
    - Logout only confirms when unsaved work exists; otherwise instant
    - Tenant layout wraps children with AuthProvider and includes SessionTimeoutModal
    - Sidebar has LogoutButton at the bottom
    - Next.js build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete auth flow end-to-end</name>
  <files>None -- verification only</files>
  <action>
    Human verifies the complete Supabase Auth flow built in Tasks 1 and 2. No code changes -- this is a visual and functional verification checkpoint.
  </action>
  <what-built>Complete Supabase Auth flow: login page, route protection, session hydration, logout with ePHI cleanup, 30-minute inactivity timeout</what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Visit http://localhost:3000 -- should redirect to /login
    3. Visit http://localhost:3000/demo-clinic/dashboard -- should redirect to /login with returnTo param
    4. On the /login page: verify glassmorphism card design, email and password fields
    5. Enter invalid credentials -- verify inline red error text appears
    6. Enter valid Supabase credentials -- verify redirect to tenant dashboard
    7. Refresh the page -- verify session persists (still logged in)
    8. Open DevTools > Application > Local Storage -- verify no Zustand devtools entries in production build
    9. Click "Log Out" in sidebar -- verify redirect to /login
    10. After logout, check localStorage -- verify ePHI keys (draft-transcript-*, encounter-*) are gone, theme preference remains
    11. (Optional) Wait 28 minutes idle to verify warning modal appears
  </how-to-verify>
  <verify>Human confirms all 10 verification steps pass</verify>
  <done>Auth flow approved by user</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. /login page renders with glassmorphism design, centered card, email/password form
2. Invalid credentials show inline red error text
3. Valid credentials redirect to tenant dashboard (or returnTo URL)
4. Unauthenticated visit to any tenant route redirects to /login?returnTo=...
5. Browser refresh preserves session (cookie-based persistence)
6. Logout clears ePHI from localStorage and resets clinical stores
7. Theme and accent preferences survive logout
8. Inactivity warning appears at 28 minutes with countdown
9. Auto-logout at 30 minutes
10. sessionStore starts null, hydrates from real Supabase JWT via onAuthStateChange
11. Next.js build succeeds
</verification>

<success_criteria>
- Users can log in with Supabase credentials and access their tenant dashboard
- Unauthenticated users cannot access any tenant route
- Sessions persist across page refresh via cookies
- Logout clears all ePHI while preserving non-PHI preferences
- Inactivity timeout protects unattended workstations (HIPAA requirement)
- No mock session data in production -- all session data comes from real JWT claims
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-auth-foundation/01-03-SUMMARY.md`
</output>
