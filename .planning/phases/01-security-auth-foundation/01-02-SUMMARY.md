---
phase: 01-security-auth-foundation
plan: 02
subsystem: infra
tags: [alembic, asyncpg, supabase-ssr, bff-proxy, jwt-hooks, migrations]

# Dependency graph
requires:
  - phase: 01-security-auth-foundation
    provides: Python backend relocated to backend/ (Plan 01)
provides:
  - Alembic async migration environment with baseline migration
  - Server-side Supabase client factory (createServerSupabaseClient)
  - BFF route handler for audit logs (GET /api/audit-logs)
  - BFF route handler for AI Scribe accept (POST /api/ai-scribe/accept)
  - Custom Access Token Hook SQL for JWT tenant_id + role injection
affects: [01-03, api-integration, session-hydration]

# Tech tracking
tech-stack:
  added: ["@supabase/ssr"]
  patterns: [bff-proxy-with-getuser, alembic-async-env, custom-access-token-hook]

key-files:
  created:
    - backend/alembic.ini
    - backend/alembic/env.py
    - backend/alembic/script.py.mako
    - backend/alembic/versions/0001_baseline.py
    - lib/supabase/server.ts
    - app/api/audit-logs/route.ts
    - app/api/ai-scribe/accept/route.ts
    - backend/db/sql/custom_access_token_hook.sql
  modified: []

key-decisions:
  - "Baseline migration is no-op -- database schema already exists via Supabase"
  - "BFF routes use getUser() for server-side auth revalidation, then getSession() for access_token forwarding"
  - "Server Supabase client uses read-only cookie adapter (setAll is no-op in route handlers)"
  - "Custom Access Token Hook queries tenant_members with LIMIT 1 for single-tenant MVP"

patterns-established:
  - "BFF proxy pattern: getUser() auth check -> getSession() for token -> fetch upstream with Bearer + 10s timeout"
  - "Server Supabase client via createServerSupabaseClient() shared across all BFF handlers"
  - "Alembic async env with dual metadata (PublicBase + TenantBase) for autogenerate"

requirements-completed: [INF-02, INF-03, INF-04, INF-05, INF-06]

# Metrics
duration: ~5min
completed: 2026-03-05
---

# Plan 01-02: Alembic + BFF Routes + Custom Access Token Hook Summary

**Alembic async migration environment with baseline, two BFF proxy route handlers using @supabase/ssr server client, and Custom Access Token Hook SQL for JWT tenant/role injection**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-03-05T21:19:21Z
- **Completed:** 2026-03-05T21:24:21Z
- **Tasks:** 2
- **Files created:** 8

## Accomplishments
- Alembic async environment configured with asyncpg driver, importing PublicBase + TenantBase metadata for autogenerate
- Baseline migration (no-op) representing current schema state, ready for future autogenerated diffs
- Server-side Supabase client factory using @supabase/ssr with cookie-based auth
- GET /api/audit-logs BFF proxy with getUser() validation, search param forwarding, 10s timeout
- POST /api/ai-scribe/accept BFF proxy with getUser() validation, encounterId extraction, 10s timeout
- Custom Access Token Hook SQL ready for Supabase dashboard registration

## Task Commits

Each task was committed atomically:

1. **Task 1: Initialize Alembic async environment and generate baseline migration** - `8bfb267` (feat)
2. **Task 2: Create BFF route handlers, server Supabase client, and Custom Access Token Hook SQL** - `c3d2c86` (feat)

## Files Created/Modified
- `backend/alembic.ini` - Alembic configuration with logging
- `backend/alembic/env.py` - Async migration environment with dual-base metadata
- `backend/alembic/script.py.mako` - Mako template for revision generation
- `backend/alembic/versions/0001_baseline.py` - No-op baseline migration
- `lib/supabase/server.ts` - createServerSupabaseClient() factory
- `app/api/audit-logs/route.ts` - GET proxy to FastAPI audit logs
- `app/api/ai-scribe/accept/route.ts` - POST proxy to FastAPI AI Scribe accept
- `backend/db/sql/custom_access_token_hook.sql` - PostgreSQL function for JWT claim injection

## Decisions Made
- Baseline migration is intentionally no-op (schema exists via Supabase)
- BFF handlers use getUser() then getSession() pattern (security + token forwarding)
- Server Supabase client is read-only for cookies (middleware handles refresh)
- Custom Access Token Hook uses LIMIT 1 for single-tenant-per-user MVP

## Deviations from Plan
None - plan executed exactly as written.

## User Setup Required

**External services require manual configuration:**
- Run `backend/db/sql/custom_access_token_hook.sql` in Supabase Dashboard -> SQL Editor
- Enable the hook in: Authentication -> Hooks -> Custom Access Token -> select `public.custom_access_token_hook`

## Next Phase Readiness
- Alembic environment ready for future schema migrations (autogenerate against live DB)
- BFF route handlers ready to proxy authenticated requests once middleware (Plan 03) is in place
- Server Supabase client shared by middleware and route handlers
- Custom Access Token Hook ready for dashboard registration -- enables JWT claim flow to session hydrator (Plan 03)

---
*Phase: 01-security-auth-foundation*
*Completed: 2026-03-05*
