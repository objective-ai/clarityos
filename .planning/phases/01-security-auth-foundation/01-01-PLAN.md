---
phase: 01-security-auth-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/main.py
  - backend/core/config.py
  - backend/core/security.py
  - backend/core/audit.py
  - backend/core/entitlements.py
  - backend/core/permissions.py
  - backend/core/__init__.py
  - backend/api/__init__.py
  - backend/api/routes/__init__.py
  - backend/api/routes/ai_scribe.py
  - backend/api/routes/audit.py
  - backend/api/routes/diagnosis.py
  - backend/api/routes/encounter.py
  - backend/api/routes/exam_findings.py
  - backend/api/routes/patient_problem.py
  - backend/api/routes/promotion.py
  - backend/api/routes/refraction.py
  - backend/api/routes/staff.py
  - backend/api/routes/vitals.py
  - backend/db/base.py
  - backend/db/session.py
  - backend/db/mixins.py
  - backend/db/models/__init__.py
  - backend/db/models/public/__init__.py
  - backend/db/models/public/saas.py
  - backend/db/models/tenant/__init__.py
  - backend/db/models/tenant/clinical.py
  - backend/schemas/__init__.py
  - backend/schemas/audit.py
  - backend/schemas/common.py
  - backend/schemas/diagnosis.py
  - backend/schemas/encounter.py
  - backend/schemas/exam_findings.py
  - backend/schemas/patient_problem.py
  - backend/schemas/refraction.py
  - backend/schemas/staff.py
  - backend/schemas/vitals.py
  - backend/__init__.py
  - next.config.mjs
  - store/sessionStore.ts
  - store/themeStore.ts
  - store/tenantCustomizationStore.ts
  - store/refractionStore.ts
  - store/encounterStore.ts
  - store/vitalsStore.ts
  - store/examFindingsStore.ts
  - store/diagnosisStore.ts
  - store/problemListStore.ts
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - SEC-09
  - SEC-10
  - INF-01

must_haves:
  truths:
    - "FastAPI refuses to start when SUPABASE_JWT_SECRET env var is missing"
    - "FastAPI refuses to start when SECRET_KEY env var is missing"
    - "No hardcoded Supabase URL exists in config.py"
    - "No Python files exist in app/ directory (only Next.js files)"
    - "All Python imports use 'from backend.*' not 'from app.*'"
    - "Security response headers (X-Frame-Options, CSP, X-Content-Type-Options) are present on every page load"
    - "Zustand devtools are not available in production builds"
  artifacts:
    - path: "backend/core/config.py"
      provides: "Pydantic Settings with required secret fields (no defaults)"
      contains: "Field(...,"
    - path: "backend/core/security.py"
      provides: "JWT verification without dev bypass"
    - path: "next.config.mjs"
      provides: "Security headers configuration"
      contains: "X-Frame-Options"
  key_links:
    - from: "backend/core/security.py"
      to: "backend/core/config.py"
      via: "settings import"
      pattern: "from backend\\.core\\.config import settings"
    - from: "backend/main.py"
      to: "backend/api/routes"
      via: "router includes"
      pattern: "from backend\\.api\\.routes"
---

<objective>
Relocate the Python backend from app/ to backend/ (resolving the Next.js namespace conflict), harden all security configurations (remove dev bypass, hardcoded secrets), add security response headers, and disable Zustand devtools in production.

Purpose: The app/ directory currently contains both Next.js pages and Python backend files, which blocks creation of BFF route handlers. Security gaps (dev bypass, hardcoded secrets) must be closed before real patient data flows. These are all prerequisite changes that unblock Plans 02 and 03.

Output: Clean app/ directory owned entirely by Next.js, hardened backend/ directory, security headers on all routes, production-safe Zustand stores.
</objective>

<execution_context>
@C:/Users/duytr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/duytr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-auth-foundation/01-CONTEXT.md
@.planning/phases/01-security-auth-foundation/01-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/core/config.py (CURRENT - will be modified):
```python
class Settings(BaseSettings):
    SUPABASE_URL: str = "https://iedzzcokfwnbyfyevjoz.supabase.co"  # HARDCODED - must become required
    SUPABASE_JWT_SECRET: str = ""  # EMPTY DEFAULT - must become required
    SECRET_KEY: str = "your-super-secret-key-for-us-saas-2026"  # HARDCODED - must become required
```

From app/core/security.py (CURRENT - lines 68-75 must be removed):
```python
if not settings.SUPABASE_JWT_SECRET:
    return TenantContext(
        user_id=UUID("a0000000-0000-0000-0000-000000000001"),
        tenant_id=UUID("b0000000-0000-0000-0000-000000000001"),
        role="doctor",
    )
```

Zustand stores using devtools (all 9):
- store/sessionStore.ts
- store/themeStore.ts
- store/tenantCustomizationStore.ts
- store/refractionStore.ts
- store/encounterStore.ts
- store/vitalsStore.ts
- store/examFindingsStore.ts
- store/diagnosisStore.ts
- store/problemListStore.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relocate Python backend to backend/ and harden security</name>
  <files>
    backend/ (entire directory - moved from app/*.py, app/core/, app/api/, app/db/, app/schemas/)
    backend/core/config.py
    backend/core/security.py
    backend/__init__.py
  </files>
  <action>
    1. Create backend/ directory at project root.

    2. Move ALL Python files and directories from app/ to backend/:
       - app/main.py -> backend/main.py
       - app/core/ -> backend/core/ (config.py, security.py, audit.py, entitlements.py, permissions.py, __init__.py)
       - app/api/ -> backend/api/ (routes/__init__.py and all route files)
       - app/db/ -> backend/db/ (base.py, session.py, mixins.py, models/)
       - app/schemas/ -> backend/schemas/ (all schema files)
       - app/__init__.py -> backend/__init__.py

    3. Delete all Python artifacts from app/:
       - Remove app/__init__.py, app/__pycache__/ and any nested __pycache__ dirs
       - Verify app/ contains ONLY: (tenant)/, favicon.ico, globals.css, layout.tsx, page.tsx
       - Do NOT delete any .tsx, .css, or .ico files

    4. Update ALL Python imports in every .py file under backend/:
       - Replace `from app.` with `from backend.`
       - Replace `import app.` with `import backend.`
       - This affects ~30 files. Use systematic find-and-replace.

    5. Harden backend/core/config.py (SEC-02, SEC-03):
       - Add `from pydantic import Field`
       - Change SUPABASE_URL to: `SUPABASE_URL: str = Field(..., description="Supabase project URL - required")`
       - Change SUPABASE_ANON_KEY to: `SUPABASE_ANON_KEY: str = Field(..., description="Supabase anon key - required")`
       - Change SUPABASE_SERVICE_ROLE_KEY to: `SUPABASE_SERVICE_ROLE_KEY: str = Field(..., description="Supabase service role key - required")`
       - Change SUPABASE_JWT_SECRET to: `SUPABASE_JWT_SECRET: str = Field(..., description="Supabase JWT secret - required")`
       - Change SECRET_KEY to: `SECRET_KEY: str = Field(..., description="App secret key - required")`
       - Keep ANTHROPIC_API_KEY as optional (empty string default) -- only needed when AI Scribe routes are called
       - Keep DATABASE_URL, DB_ECHO_SQL, CORS_ORIGINS, ACCESS_TOKEN_EXPIRE_MINUTES with their existing defaults

    6. Harden backend/core/security.py (SEC-01):
       - Remove the entire dev bypass block (lines 68-75 in original): the `if not settings.SUPABASE_JWT_SECRET:` conditional and its TenantContext return
       - The function should go straight from `token = credentials.credentials` to the JWT decode try/except
       - Since config.py now requires SUPABASE_JWT_SECRET (no default), startup fails if unset -- no bypass possible

    7. Ensure all backend/ subdirectories have __init__.py files for Python package discovery.
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(f).read()) for f in __import__('glob').glob('backend/**/*.py', recursive=True)]" && echo "All Python files parse OK" && python -c "import pathlib; py_files = list(pathlib.Path('app').rglob('*.py')); assert len(py_files) == 0, f'Python files still in app/: {py_files}'" && echo "No Python in app/" && python -c "
import pathlib
for f in pathlib.Path('backend').rglob('*.py'):
    content = f.read_text()
    if 'from app.' in content or 'import app.' in content:
        print(f'BAD IMPORT in {f}')
        exit(1)
print('All imports updated')
" && echo "PASS"</automated>
  </verify>
  <done>
    - All Python files moved from app/ to backend/
    - app/ contains only Next.js files (no .py, no __init__.py, no __pycache__)
    - All `from app.*` imports replaced with `from backend.*`
    - config.py: SUPABASE_URL, SUPABASE_JWT_SECRET, SECRET_KEY, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY all use Field(...) with no defaults
    - security.py: dev bypass block completely removed
    - All .py files parse without syntax errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add security headers and disable Zustand devtools in production</name>
  <files>
    next.config.mjs
    store/sessionStore.ts
    store/themeStore.ts
    store/tenantCustomizationStore.ts
    store/refractionStore.ts
    store/encounterStore.ts
    store/vitalsStore.ts
    store/examFindingsStore.ts
    store/diagnosisStore.ts
    store/problemListStore.ts
  </files>
  <action>
    1. Update next.config.mjs (SEC-09) to add security headers. Add an async headers() function returning an array with a single entry for source '/(.*)', containing these headers:
       - X-Frame-Options: DENY
       - X-Content-Type-Options: nosniff
       - Referrer-Policy: strict-origin-when-cross-origin
       - Permissions-Policy: camera=(), microphone=(self), geolocation=()
       - Content-Security-Policy: "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' blob: data: https://*.supabase.co; font-src 'self'; connect-src 'self' https://*.supabase.co wss://*.supabase.co; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'; upgrade-insecure-requests"
       - Do NOT include 'unsafe-eval' in production CSP -- Next.js does not need it for prod builds
       - Keep the existing nextConfig object structure, just add the headers() method

    2. Update ALL 9 Zustand stores to conditionally apply devtools (SEC-10). For each store:
       - Add at the top of the file: `const isDev = process.env.NODE_ENV === 'development'`
       - Replace the direct `devtools(...)` wrapping with a conditional pattern:
         - In development: `devtools(storeImpl, { name: 'ClarityOS/StoreName' })`
         - In production: just `storeImpl` (no devtools wrapper)
       - Use this pattern for the conditional:
         ```typescript
         const storeImpl = (set, get) => ({ /* existing store body */ })
         export const useXxxStore = create<XxxState>()(
           isDev ? devtools(storeImpl, { name: 'ClarityOS/Xxx' }) : storeImpl
         )
         ```
       - Keep all existing store logic, selectors, and exports unchanged
       - The 9 stores are: sessionStore, themeStore, tenantCustomizationStore, refractionStore, encounterStore, vitalsStore, examFindingsStore, diagnosisStore, problemListStore
  </action>
  <verify>
    <automated>cd "C:/Users/duytr/OneDrive/Desktop/clarityos-erp" && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - next.config.mjs contains headers() with X-Frame-Options DENY, X-Content-Type-Options nosniff, CSP, Referrer-Policy, Permissions-Policy
    - All 9 Zustand stores conditionally apply devtools only when NODE_ENV === 'development'
    - Next.js build succeeds without errors
  </done>
</task>

</tasks>

<verification>
1. No Python files remain in app/ directory
2. All Python files parse cleanly under backend/
3. No `from app.` imports exist in any Python file
4. config.py has no hardcoded secret defaults (grep for 'your-super-secret' and 'iedzzcokfwnbyfyevjoz' returns nothing)
5. security.py has no dev bypass (grep for 'Development bypass' returns nothing)
6. Next.js build succeeds
7. Security headers present in next.config.mjs
8. No store file has unconditional devtools() in production
</verification>

<success_criteria>
- Python backend lives entirely in backend/ with clean imports
- FastAPI would refuse to start without required env vars (SUPABASE_JWT_SECRET, SECRET_KEY, SUPABASE_URL)
- Dev auth bypass is eliminated from security.py
- All HTTP responses include security headers (CSP, X-Frame-Options, X-Content-Type-Options)
- Zustand devtools disabled in production builds
- Next.js build passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-auth-foundation/01-01-SUMMARY.md`
</output>
