---
phase: 10.3-error-monitoring-system-status
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/core/sentry_setup.py
  - backend/tests/test_sentry_init.py
  - backend/tests/test_clinical_tagger.py
  - backend/main.py
autonomous: true
requirements: [PHASE-10.3-CONTEXT]
gap_closure: false
user_setup:
  - service: sentry
    why: "Error capture for FastAPI backend"
    env_vars:
      - name: SENTRY_DSN
        source: "Sentry Dashboard (same DSN as Next.js or separate project)"
      - name: SENTRY_ENVIRONMENT
        source: "Set to 'production' only on prod deploy"
      - name: GIT_SHA
        source: "Baked at Docker build time via ARG GIT_SHA and ENV GIT_SHA=$GIT_SHA"

must_haves:
  truths:
    - "sentry-sdk[fastapi] is declared in backend/requirements.txt"
    - "init_sentry() is called from backend/main.py immediately after app = FastAPI(...) and BEFORE any add_middleware/include_router calls"
    - "init_sentry() is a no-op when SENTRY_DSN is unset OR SENTRY_ENVIRONMENT != 'production'"
    - "_before_send composes: ignore-rules + clinical body-drop + deny-list scrub via scrub_event from Plan 01"
    - "Unit tests verify env-gate AND that every clinical prefix triggers tag+body-drop"
  artifacts:
    - path: "backend/core/sentry_setup.py"
      provides: "init_sentry() + _before_send wrapper that delegates to scrub_event"
      min_lines: 60
    - path: "backend/tests/test_sentry_init.py"
      provides: "Env-gate + init wiring tests with mocked sentry_sdk"
      min_lines: 60
    - path: "backend/tests/test_clinical_tagger.py"
      provides: "One assertion per clinical prefix — tag set, request.data dropped"
      min_lines: 60
  key_links:
    - from: "backend/main.py"
      to: "backend/core/sentry_setup.py"
      via: "from backend.core.sentry_setup import init_sentry; init_sentry() right after app = FastAPI(...)"
      pattern: "init_sentry\\(\\)"
    - from: "backend/core/sentry_setup.py"
      to: "backend/core/sentry_scrubber.py"
      via: "from backend.core.sentry_scrubber import scrub_event, CLINICAL_PREFIXES"
      pattern: "from backend.core.sentry_scrubber import"
---

<objective>
Wire sentry-sdk[fastapi] into the FastAPI backend with the PHI scrubber + clinical-route body-drop + ignore rules all composed through before_send. Init must be env-gated (prod only) and called BEFORE any middleware or router registration so StarletteIntegration wraps every unhandled exception.

Purpose: Backend errors reach Sentry only after scrub_event has walked the payload, dropped clinical bodies, and filtered 401/403/ClientDisconnect. Everywhere else, Sentry is silent.

Output: backend/core/sentry_setup.py, two new pytest test files, backend/requirements.txt updated, backend/main.py calls init_sentry() at the correct position.
</objective>

<execution_context>
@C:/Users/duytr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/duytr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10.3-error-monitoring-system-status/10.3-CONTEXT.md
@.planning/phases/10.3-error-monitoring-system-status/10.3-RESEARCH.md
@.planning/phases/10.3-error-monitoring-system-status/10.3-VALIDATION.md
@.claude/rules/clinical-safety.md
@.claude/rules/backend-python.md

<interfaces>
From backend.core.sentry_scrubber (Plan 01):

    def scrub_event(event: dict, hint: dict | None = None) -> dict | None: ...
    CLINICAL_PREFIXES: tuple[str, ...]  # 7 paths
    DENY_KEYS: frozenset[str]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sentry-sdk[fastapi] to requirements + create init_sentry()</name>
  <files>backend/requirements.txt, backend/core/sentry_setup.py</files>
  <read_first>
    - backend/requirements.txt (current deps — confirm sentry-sdk not already pinned)
    - backend/core/sentry_scrubber.py (Plan 01 — confirm scrub_event + CLINICAL_PREFIXES exported)
    - .planning/phases/10.3-error-monitoring-system-status/10.3-RESEARCH.md (section "Pattern 2: FastAPI Sentry init")
    - .planning/phases/10.3-error-monitoring-system-status/10.3-CONTEXT.md (sections "Ignore rules" and "Clinical route tagging")
  </read_first>
  <action>
    Pre-check: `test -f backend/core/sentry_scrubber.py` MUST be true. If missing, STOP — Plan 01 has not landed.

    1. Update backend/requirements.txt: append the line `sentry-sdk[fastapi]>=2.0,<3.0`. Do NOT run pip install now; Docker build / deploy handles the install.

    2. Create backend/core/sentry_setup.py with this exact structure:

        from __future__ import annotations
        import os
        from typing import Any, Optional
        from backend.core.sentry_scrubber import scrub_event, CLINICAL_PREFIXES  # noqa: F401

        def _before_send(event: dict[str, Any], hint: dict[str, Any]) -> Optional[dict[str, Any]]:
            # scrub_event already composes ignore-rules + clinical body-drop + deny-list scrub.
            # The wrapper keeps init unit-testable without importing sentry_sdk at module scope.
            return scrub_event(event, hint)

        def init_sentry() -> None:
            dsn = os.getenv("SENTRY_DSN")
            env = os.getenv("SENTRY_ENVIRONMENT")
            if not dsn or env != "production":
                return
            # Lazy imports so unit tests can import this module without sentry-sdk installed locally.
            import sentry_sdk
            from sentry_sdk.integrations.fastapi import FastApiIntegration
            from sentry_sdk.integrations.starlette import StarletteIntegration
            from sentry_sdk.integrations.asyncio import AsyncioIntegration
            sentry_sdk.init(
                dsn=dsn,
                environment="production",
                release=os.getenv("GIT_SHA"),
                traces_sample_rate=0.0,
                send_default_pii=False,
                max_request_body_size="small",
                integrations=[
                    StarletteIntegration(transaction_style="endpoint"),
                    FastApiIntegration(transaction_style="endpoint"),
                    AsyncioIntegration(),
                ],
                before_send=_before_send,
            )

    3. Verify module imports without sentry-sdk installed locally (the sentry_sdk imports are inside init_sentry so import-time is clean):
        `cd backend && python -c "from backend.core.sentry_setup import init_sentry, _before_send; print('OK')"`

    Commit: `feat(10.3-03): add FastAPI sentry init with composed before_send`
  </action>
  <verify>
    <automated>cd C:/Users/duytr/Projects/clarityos/backend && grep -q 'sentry-sdk' requirements.txt && test -f core/sentry_setup.py && grep -q 'from backend.core.sentry_scrubber import scrub_event' core/sentry_setup.py && grep -q 'def init_sentry' core/sentry_setup.py && grep -q 'def _before_send' core/sentry_setup.py && python -c "from backend.core.sentry_setup import init_sentry, _before_send; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/requirements.txt contains a line matching `sentry-sdk[fastapi]>=2.0,<3.0`
    - backend/core/sentry_setup.py exists
    - grep -q 'def init_sentry' backend/core/sentry_setup.py
    - grep -q 'def _before_send' backend/core/sentry_setup.py
    - grep -q 'from backend.core.sentry_scrubber import scrub_event' backend/core/sentry_setup.py
    - grep -q 'if not dsn or env != "production"' backend/core/sentry_setup.py
    - grep -q 'traces_sample_rate=0.0' backend/core/sentry_setup.py
    - grep -q 'send_default_pii=False' backend/core/sentry_setup.py
    - grep -q 'StarletteIntegration' backend/core/sentry_setup.py AND grep -q 'FastApiIntegration' backend/core/sentry_setup.py
    - `python -c "from backend.core.sentry_setup import init_sentry, _before_send"` exits 0
    - Commit exists with "feat(10.3-03)"
  </acceptance_criteria>
  <done>sentry_setup module in place, env-gated, delegates to scrub_event; requirements updated.</done>
</task>

<task type="auto">
  <name>Task 2: Write init + clinical-tagger unit tests</name>
  <files>backend/tests/test_sentry_init.py, backend/tests/test_clinical_tagger.py</files>
  <read_first>
    - backend/core/sentry_setup.py (Task 1 output — confirm function names match imports)
    - backend/core/sentry_scrubber.py (Plan 01 — confirm CLINICAL_PREFIXES shape)
    - backend/tests/test_sentry_scrubber.py (Plan 01 — pattern to reuse for fixtures)
    - .planning/phases/10.3-error-monitoring-system-status/10.3-VALIDATION.md (tasks 10.3-03-01 and 10.3-03-02)
  </read_first>
  <behavior>
    test_sentry_init.py MUST cover:
      - test_init_skipped_when_dsn_missing: SENTRY_DSN unset → sentry_sdk.init NOT called
      - test_init_skipped_in_dev: SENTRY_DSN set, SENTRY_ENVIRONMENT="development" → NOT called
      - test_init_skipped_in_preview: SENTRY_DSN set, SENTRY_ENVIRONMENT="preview" → NOT called
      - test_init_runs_in_prod: SENTRY_DSN set, SENTRY_ENVIRONMENT="production" → sentry_sdk.init called once with before_send=_before_send, traces_sample_rate=0.0, send_default_pii=False
      - test_init_sets_release_from_git_sha: GIT_SHA="abc123" → init kwargs include release="abc123"

    test_clinical_tagger.py MUST cover (using _before_send directly, no sentry_sdk dep):
      - One test per CLINICAL_PREFIX (7 tests): event with request.url starting with prefix + request.data={"soap_text": "PHI"} → result["tags"]["route_type"] == "clinical" AND "data" not in result["request"]
      - test_non_clinical_route_untouched: request.url = "/api/staff" + request.data={"notes": "X"} → result has no route_type=clinical tag and notes key is redacted (via deny-list) but the request.data dict itself still exists
  </behavior>
  <action>
    Create backend/tests/test_sentry_init.py:

        import os
        from unittest.mock import patch, MagicMock
        import pytest
        from backend.core import sentry_setup

        def _run_init_with_env(env):
            with patch.dict(os.environ, env, clear=True):
                fake_sdk = MagicMock()
                fake_fi = MagicMock()
                fake_si = MagicMock()
                fake_ai = MagicMock()
                with patch.dict("sys.modules", {
                    "sentry_sdk": fake_sdk,
                    "sentry_sdk.integrations.fastapi": MagicMock(FastApiIntegration=fake_fi),
                    "sentry_sdk.integrations.starlette": MagicMock(StarletteIntegration=fake_si),
                    "sentry_sdk.integrations.asyncio": MagicMock(AsyncioIntegration=fake_ai),
                }):
                    sentry_setup.init_sentry()
                return fake_sdk

        def test_init_skipped_when_dsn_missing():
            fake = _run_init_with_env({"SENTRY_ENVIRONMENT": "production"})
            fake.init.assert_not_called()

        def test_init_skipped_in_dev():
            fake = _run_init_with_env({"SENTRY_DSN": "https://x@o.ingest.sentry.io/1", "SENTRY_ENVIRONMENT": "development"})
            fake.init.assert_not_called()

        def test_init_skipped_in_preview():
            fake = _run_init_with_env({"SENTRY_DSN": "https://x@o.ingest.sentry.io/1", "SENTRY_ENVIRONMENT": "preview"})
            fake.init.assert_not_called()

        def test_init_runs_in_prod():
            fake = _run_init_with_env({"SENTRY_DSN": "https://x@o.ingest.sentry.io/1", "SENTRY_ENVIRONMENT": "production", "GIT_SHA": "abc123"})
            fake.init.assert_called_once()
            kwargs = fake.init.call_args.kwargs
            assert kwargs["environment"] == "production"
            assert kwargs["traces_sample_rate"] == 0.0
            assert kwargs["send_default_pii"] is False
            assert kwargs["before_send"] is sentry_setup._before_send
            assert kwargs["release"] == "abc123"

    Create backend/tests/test_clinical_tagger.py:

        import pytest
        from backend.core.sentry_setup import _before_send
        from backend.core.sentry_scrubber import CLINICAL_PREFIXES

        @pytest.mark.parametrize("prefix", list(CLINICAL_PREFIXES))
        def test_clinical_body_dropped_for_each_prefix(prefix):
            event = {
                "request": {
                    "url": f"https://api.example.com{prefix}/123",
                    "data": {"soap_text": "PATIENT PHI"},
                }
            }
            result = _before_send(event, {})
            assert result is not None
            assert result["tags"]["route_type"] == "clinical"
            assert "data" not in result["request"]

        def test_non_clinical_route_untouched():
            event = {"request": {"url": "https://api.example.com/api/staff", "data": {"notes": "X"}}}
            result = _before_send(event, {})
            # deny-list still redacts 'notes' but the tag is not set
            assert result.get("tags", {}).get("route_type") != "clinical"
            # data dict is still present (body-drop did not fire)
            assert "data" in result["request"]
            assert result["request"]["data"]["notes"] == "[Filtered]"

    Run: `cd backend && python -m pytest tests/test_sentry_init.py tests/test_clinical_tagger.py -v`

    All MUST pass (exit 0; ~13 tests total: 5 init + 7 prefix + 1 non-clinical).

    Commit: `test(10.3-03): add sentry init + clinical-tagger unit tests`
  </action>
  <verify>
    <automated>cd C:/Users/duytr/Projects/clarityos/backend && python -m pytest tests/test_sentry_init.py tests/test_clinical_tagger.py -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_sentry_init.py exists and contains (grep): test_init_skipped_when_dsn_missing, test_init_skipped_in_dev, test_init_skipped_in_preview, test_init_runs_in_prod
    - backend/tests/test_clinical_tagger.py exists and uses @pytest.mark.parametrize with CLINICAL_PREFIXES
    - pytest reports >= 12 passed, 0 failed
    - Exit code 0
    - Commit exists with "test(10.3-03)"
  </acceptance_criteria>
  <done>init + clinical-tagger tests pass locally.</done>
</task>

<task type="auto">
  <name>Task 3: Call init_sentry() in backend/main.py at the correct position</name>
  <files>backend/main.py</files>
  <read_first>
    - backend/main.py (find the exact line where `app = FastAPI(...)` is instantiated — init_sentry() goes IMMEDIATELY after that line and BEFORE any app.add_middleware() / app.include_router() calls)
    - backend/core/sentry_setup.py (Task 1 output — confirm init_sentry symbol)
    - .planning/phases/10.3-error-monitoring-system-status/10.3-CONTEXT.md (section "Integration Points" — FastAPI middleware order requirement)
    - .planning/phases/10.3-error-monitoring-system-status/10.3-RESEARCH.md (section "Pattern 2" — the "immediately after app = FastAPI" rule)
  </read_first>
  <action>
    1. Read backend/main.py and identify:
       - The line `app = FastAPI(...)` (capture its line number).
       - The FIRST subsequent `app.add_middleware(...)` OR `app.include_router(...)` line.
       - init_sentry() MUST be called on a line between these two anchors.

    2. Add the import near the top of backend/main.py (grouped with other `from backend.core...` imports):

        from backend.core.sentry_setup import init_sentry

    3. Insert `init_sentry()` as a standalone statement IMMEDIATELY after the `app = FastAPI(...)` constructor expression (or at end of that FastAPI(...) call if multi-line). Add a brief inline comment:

        app = FastAPI(...)
        init_sentry()  # MUST be before add_middleware/include_router — Sentry ASGI wraps the app on init

    4. Verify the order with grep — init_sentry line number MUST be less than ALL add_middleware and include_router line numbers:

        cd backend && python -c "
        import re
        src = open('main.py').read().splitlines()
        init_line = next(i for i, l in enumerate(src) if 'init_sentry()' in l)
        mw_lines = [i for i, l in enumerate(src) if 'add_middleware' in l or 'include_router' in l]
        assert all(init_line < m for m in mw_lines), 'init_sentry must precede all middleware/router calls'
        print('ORDER OK')
        "

    5. Confirm the app still boots cleanly. In dev mode (no SENTRY_DSN), init_sentry() is a no-op — the app starts normally:

        bash scripts/dev.sh check-api 2>&1 | tail -5
        # OR if API is down:
        bash scripts/dev.sh ensure-api 2>&1 | tail -10

    Commit: `feat(10.3-03): call init_sentry() in main.py before middleware registration`
  </action>
  <verify>
    <automated>cd C:/Users/duytr/Projects/clarityos/backend && grep -q 'from backend.core.sentry_setup import init_sentry' main.py && grep -q 'init_sentry()' main.py && python -c "src = open('main.py').read().splitlines(); i = next(n for n,l in enumerate(src) if 'init_sentry()' in l); ms = [n for n,l in enumerate(src) if 'add_middleware' in l or 'include_router' in l]; assert ms, 'no middleware/routers found'; assert all(i < m for m in ms), f'init at {i} must precede all middleware/routers {ms}'; print('ORDER OK')"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'from backend.core.sentry_setup import init_sentry' backend/main.py
    - grep -q 'init_sentry()' backend/main.py (exactly one call site)
    - The line number of `init_sentry()` is STRICTLY LESS THAN every `add_middleware(...)` and `include_router(...)` line number in the same file
    - `bash scripts/dev.sh check-api` returns healthy OR `bash scripts/dev.sh ensure-api` starts cleanly (no startup crash from Sentry imports)
    - Existing middleware/router registrations remain byte-identical (git diff shows only the import + single init call added — no reordering of existing lines)
    - Commit exists with "feat(10.3-03)" and "main.py"
  </acceptance_criteria>
  <done>init_sentry() called in correct position; no startup regression.</done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/test_sentry_init.py tests/test_clinical_tagger.py tests/test_sentry_scrubber.py -v` — ALL green
- `grep -q 'init_sentry()' backend/main.py` and order check passes
- `bash scripts/dev.sh check-api` healthy after change
- Confirm the init is a no-op locally (SENTRY_DSN unset): start backend, verify no sentry_sdk messages in logs
</verification>

<success_criteria>
- FastAPI backend can initialize Sentry in prod only
- before_send composes all three filter stages (ignore → body-drop → scrub)
- init happens at the right position in main.py
- All tests pass; no startup regression
</success_criteria>

<output>
Create `.planning/phases/10.3-error-monitoring-system-status/10.3-03-SUMMARY.md` with:
- Exact sentry-sdk pin line added to requirements.txt
- init_sentry call site line number in main.py and confirmation it precedes all middleware/routers
- pytest counts for test_sentry_init + test_clinical_tagger
- Any deviations from the plan (e.g. if main.py structure required adapting the insertion strategy)
</output>
