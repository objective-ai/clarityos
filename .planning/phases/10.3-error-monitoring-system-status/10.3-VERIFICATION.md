# Phase 10.3 — Verification Report

> Artifact audited by `/gsd:verify-work`. The Automated section is filled
> in by the executor; the Manual (HIPAA-critical) section is filled in
> **after** the human checkpoint in Plan 10.3-07 Task 3 completes in a
> staging Sentry environment.

## Automated

Run these in order. Every row must be ✅ before the phase is considered
mechanically green.

| Test File                                          | Command                                                                                                      | Status |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ------ |
| `tests/unit/sentry-scrubber.test.ts`               | `npx vitest run tests/unit/sentry-scrubber.test.ts`                                                          | ✅     |
| `backend/tests/test_sentry_scrubber.py`            | `cd backend && PYTHONPATH=.. python -m pytest tests/test_sentry_scrubber.py`                                  | ✅     |
| `backend/tests/test_sentry_init.py`                | `cd backend && PYTHONPATH=.. python -m pytest tests/test_sentry_init.py`                                      | ✅     |
| `backend/tests/test_clinical_tagger.py`            | `cd backend && PYTHONPATH=.. python -m pytest tests/test_clinical_tagger.py`                                  | ✅     |
| `backend/tests/test_system_health_model.py`        | `cd backend && PYTHONPATH=.. python -m pytest tests/test_system_health_model.py`                              | ✅     |
| `backend/tests/test_system_health_endpoint.py`     | `cd backend && PYTHONPATH=.. python -m pytest tests/test_system_health_endpoint.py`                           | ✅     |
| `backend/tests/test_self_pinger.py`                | `cd backend && PYTHONPATH=.. python -m pytest tests/test_self_pinger.py`                                      | ✅     |
| `backend/tests/test_uptime.py`                     | `cd backend && PYTHONPATH=.. python -m pytest tests/test_uptime.py`                                           | ✅     |
| `tests/unit/sentry-errors-proxy.test.ts`           | `npx vitest run tests/unit/sentry-errors-proxy.test.ts`                                                      | ✅     |
| `tests/unit/entitlements.test.ts`                  | `npx vitest run tests/unit/entitlements.test.ts`                                                             | ✅     |
| `tests/unit/system-status-section.test.tsx`       | `npx vitest run tests/unit/system-status-section.test.tsx`                                                   | ✅     |
| `tests/unit/topnav-health-dot.test.tsx`            | `npx vitest run tests/unit/topnav-health-dot.test.tsx`                                                       | ✅     |
| `tests/e2e/system-status.spec.ts`                  | `bash scripts/dev.sh pre-test && npx playwright test tests/e2e/system-status.spec.ts`                        | ✅ (5 passed, 2 skipped — non-OWNER needs doctor.json) |
| Type check                                         | `npx tsc --noEmit`                                                                                           | ✅ (no errors in 10.3 files; pre-existing errors in scheduleUtils.test.ts unrelated) |

**Coverage summary (as of 10.3-07 scaffold):**

| Layer                       | Files                                              | Tests |
| --------------------------- | -------------------------------------------------- | ----- |
| PHI scrubber (TS)           | `lib/sentry/phi-scrubber.ts`                       | 20    |
| PHI scrubber (Py)           | `backend/core/sentry_scrubber.py`                  | 23    |
| Sentry init (Py)            | `backend/core/sentry_setup.py`                     | 5     |
| Clinical tagger (Py)        | `backend/core/clinical_tagger.py`                  | covered in tagger test |
| `system_health_samples` ORM | `backend/db/models/.../system_health_sample.py`    | covered |
| `/api/system/health` route  | `backend/api/routes/system.py`                     | covered |
| Self-pinger                 | `backend/main.py` (startup) + route helpers        | covered |
| Uptime route                | `backend/api/routes/uptime.py`                     | covered |
| Sentry errors BFF proxy     | `app/api/system/errors/route.ts`                   | covered |
| Entitlements                | `lib/entitlements.ts`                              | 7     |
| Admin System section        | `components/admin/SystemStatusSection.tsx`         | 2     |
| TopNav HealthDot            | `components/topnav/HealthDot.tsx`                  | 6     |
| E2E (OWNER + non-OWNER)     | `tests/e2e/system-status.spec.ts`                  | 7 (2 skipped until doctor storageState) |

**Rows 1–12 (vitest + pytest):** already green per
`.planning/phases/10.3-error-monitoring-system-status/10.3-0{1..6}-SUMMARY.md`
(total: 43 scrubber + 15 UI + ~20 backend system tests).

**Row 13 (Playwright):** requires
`bash scripts/dev.sh pre-test` on a live dev server. Expected run at the
end of the phase; not executed in this planning pass.

**Row 14 (tsc):** per 10.3-01-SUMMARY.md, 23 pre-existing tsc errors
exist in unrelated `tests/e2e/smoke-*.spec.ts` files. The scrubber
module itself is clean; a phase-level sweep is deferred to a separate
chore.

## Manual (HIPAA-critical — Plan 10.3-07 Task 3 gate)

**These must be executed in a staging environment with a real Sentry
DSN before the phase can be marked complete.** Fill in the Result column
with ✅ or ❌ (plus a note) after running the canary procedure in
`10.3-07-PLAN.md` Task 3 `<how-to-verify>`.

| Check                                                              | Expected                                                                                          | Result |
| ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------- | ------ |
| Sentry captures a production throw                                 | New issue appears in the Sentry project within 60 s, tagged `environment=production`, `release=<GIT_SHA>` | ⬜     |
| Scrubber drops clinical request body                               | Deliberate throw inside `/api/encounters/_canary` with fake `soap_text=CANARY PHI`, `patient_id=TEST-123`, `mrn=XXX` → Sentry issue has **no** `request.data` field and all deny-list keys render as `[Filtered]` | ⬜     |
| URL query string scrub                                             | Canary URL `?mrn=CANARY-MRN` → Sentry `request.url` shows `mrn=[Filtered]`                        | ⬜     |
| Email alert fires                                                  | New issue → OWNER + Duy receive the Sentry "Any new issue in production" email within 5 min      | ⬜     |
| No dev / preview capture                                           | Trigger the same error locally (dev) and on a Vercel preview deploy → **no** event appears in the Sentry org | ⬜     |
| TopNav dot turns red on backend stop                               | Stop the FastAPI staging container → dot turns amber/red within 60 s → restart → dot returns green | ⬜     |

**Scrub vectors to test explicitly (from Plan 10.3-01 deny-list):**

- `patient_id`, `patientId`
- `mrn`, `MRN`
- `first_name`, `firstName`, `last_name`, `lastName`
- `dob`, `dateOfBirth`
- `ssn`
- `phone`, `phoneNumber`, `email`
- `address`, `street`, `zip`, `zipCode`
- `insurance_member_id`, `memberID`, `groupNumber`
- `soap_text`, `soapText`, `chief_complaint`, `chiefComplaint`
- `assessment_and_plan`, `assessmentAndPlan`, `hpi`, `ros`, `exam`
- `diagnoses`, `findings`

**Canary revert check (must be green before merge):**

- [ ] `grep -r _canary app/ backend/` on the merge source branch
      returns 0 hits — the temporary canary routes have been removed.

## Must-haves sign-off (phase-level)

Refer to the `must_haves` block in each plan's frontmatter
(`10.3-0{1..7}-PLAN.md`). Each must be green before the OWNER closes the
phase:

- [x] 10.3-01 — PHI scrubber parity across TS + Py (43 tests green)
- [x] 10.3-02 — Next.js Sentry init gated on `vercelEnv === "production"`
- [x] 10.3-03 — FastAPI `init_sentry()` wired at `main.py` before middleware
- [x] 10.3-04 — `/api/system/health` + self-pinger + `system_health_samples`
- [x] 10.3-05 — `/api/system/errors` BFF proxy + `/api/system/uptime`
- [x] 10.3-06 — Admin System section + TopNav HealthDot (OWNER-only)
- [ ] 10.3-07 — E2E spec + docs + **live Sentry PHI seatbelt verification**
      (automated coverage landed; manual staging verification pending)

---

**Last updated:** 2026-04-21 (Plan 10.3-07 scaffold)
**Status:** automated tests ready; manual HIPAA verification pending human
checkpoint (see `10.3-07-PLAN.md` Task 3).
