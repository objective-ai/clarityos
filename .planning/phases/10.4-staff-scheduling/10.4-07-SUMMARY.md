---
phase: 10.4-staff-scheduling
plan: "07"
subsystem: testing
tags: [e2e, playwright, pytest, timezone, staff-scheduling]
dependency_graph:
  requires: ["10.4-05", "10.4-06"]
  provides: ["e2e-staff-schedule", "e2e-clock-in-out", "unit-tz-derivation"]
  affects: []
tech_stack:
  added: []
  patterns: ["data-testid selectors only", "ZoneInfo-typed tz args", "graceful skip on empty staff API"]
key_files:
  created:
    - tests/e2e/staff-schedule.spec.ts
    - tests/e2e/clock-in-out.spec.ts
    - backend/tests/test_staff_schedule_tz.py
  modified: []
decisions:
  - "Skip gracefully in staff-schedule.spec.ts when staff API returns empty pills (dev user edge case) rather than failing"
  - "Pre-existing tsc errors in other spec files are out of scope — new files are clean"
  - "pytest must be run with root .env sourced (env vars required by settings import chain)"
metrics:
  duration_minutes: 15
  completed_date: "2026-04-21"
  tasks_completed: 3
  files_created: 3
  files_modified: 0
---

# Phase 10.4 Plan 07: E2E Tests + Backend Unit Tests — Summary

**One-liner:** Playwright E2E specs for admin schedule editor and TopNav clock-in/out (data-testid selectors only), plus 5 pytest cases proving `_clinic_date_from_utc` handles UTC/LA/Auckland/naive datetime correctly with ZoneInfo objects.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Admin schedule tab save/reload E2E | c583024 | tests/e2e/staff-schedule.spec.ts |
| 2 | TopNav clock-in/out E2E | b203aec | tests/e2e/clock-in-out.spec.ts |
| 3 | Pytest tz unit tests + tsc verification | 931c0c1 | backend/tests/test_staff_schedule_tz.py |

## Test File Details

### tests/e2e/staff-schedule.spec.ts
- **Selectors used:** all `data-testid`-based
  - `[data-testid^="schedule-provider-pill-"]` — provider pill selection
  - `[data-testid="schedule-day-toggle-0"]` — Monday toggle
  - `[data-testid="schedule-day-start-0"]` — Monday start time input
  - `[data-testid="schedule-day-end-0"]` — Monday end time input
  - `[data-testid="schedule-save-weekly"]` — save button
- **Nav step:** `page.locator("nav button").filter({ hasText: "Schedule" })` — acceptable (navigation step, not assertion target)
- **Graceful skip:** If staff API returns 0 provider pills, test skips with explanation rather than failing
- **Testid → component mapping:** All testids confirmed present in `components/admin/ScheduleSection.tsx`

### tests/e2e/clock-in-out.spec.ts
- **Selectors used:** all `data-testid`-based
  - `[data-testid="topnav-clock-button"]` — the clock button in TopNav
  - `[data-testid="topnav-clock-elapsed"]` — elapsed timer (visible only when clocked in)
- **State reset:** Checks if already clocked in, clocks out first for idempotency
- **Assertions:** elapsed appears with `/\d{2}:\d{2}/` format; disappears after clock-out
- **Testid → component mapping:** Both testids confirmed in `components/topnav/ClockInButton.tsx`

### backend/tests/test_staff_schedule_tz.py
- **5 test cases:**
  1. `test_clinic_date_utc_midnight_is_same_day` — UTC midnight stays same day
  2. `test_clinic_date_la_evening_same_as_utc_next_day` — 02:30 UTC = 19:30 PDT previous day
  3. `test_clinic_date_auckland_morning_ahead_of_utc` — 22:00 UTC = 10:00 NZST next day
  4. `test_clinic_date_naive_datetime_treated_as_utc` — naive dt gets UTC tzinfo filled
  5. `test_clinic_date_la_noon_utc_is_la_morning` — 12:00 UTC = 05:00 PDT, same calendar day
- **ZoneInfo usage:** All 5 calls pass `ZoneInfo(...)` objects — no bare strings
- **Run command:** `cd backend && set -a && source ../.env && set +a && PYTHONPATH=c:/Users/duytr/Projects/clarityos pytest tests/test_staff_schedule_tz.py -v`
- **Result:** 5/5 PASSED

## Verification Results

| Check | Result |
|-------|--------|
| pytest 5 cases | PASSED |
| New TS files tsc errors | CLEAN (0 errors) |
| staff-schedule.spec.ts — no xpath/:has-text primary selectors | PASS |
| clock-in-out.spec.ts — no xpath/getByRole primary selectors | PASS |
| ZoneInfo count in pytest file | 5 (meets >= 5 requirement) |
| No bare string tz calls | PASS (0 matches for `_clinic_date_from_utc(dt, "`) |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] pytest failed with missing env vars**
- **Found during:** Task 3 execution
- **Issue:** `api.routes.staff_schedule` imports `backend.core.config.settings` which requires DATABASE_URL, SUPABASE_URL, etc. at import time. Running pytest without env vars caused `ValidationError: 6 validation errors for Settings`.
- **Fix:** Source root `.env` before running pytest: `set -a && source ../.env && set +a`
- **Files modified:** None — run-command fix only
- **Commit:** N/A (documented in task notes)

**2. [Out of scope] Pre-existing tsc errors**
- `lib/scheduleUtils.test.ts` — `checkedInAt` missing in type (pre-existing)
- Multiple e2e specs — unused variables (pre-existing)
- **Action:** Logged, not fixed. New files are clean.

## must_haves Truth Checklist (all 7 plans)

| Truth | Test that proves it |
|-------|-------------------|
| Admin can save/reload weekly schedule | `staff-schedule.spec.ts` — fills 08:30/17:30, reloads, asserts persistence |
| Staff can clock in and clock out from TopNav | `clock-in-out.spec.ts` — clicks topnav-clock-button, asserts elapsed visible, clicks out |
| Clinic-timezone date derivation handles UTC/LA/Auckland/naive | `test_staff_schedule_tz.py` — 5 pytest cases |
| ZoneInfo objects used (not strings) | All 5 `_clinic_date_from_utc` calls use `ZoneInfo(...)` |
| data-testid selectors exclusively (no xpath/:has-text) | Grep confirms clean in both spec files |
| tsc passes on new files | New files: 0 tsc errors |

## Self-Check: PASSED

- FOUND: tests/e2e/staff-schedule.spec.ts
- FOUND: tests/e2e/clock-in-out.spec.ts
- FOUND: backend/tests/test_staff_schedule_tz.py
- FOUND: commit c583024 (staff-schedule spec)
- FOUND: commit b203aec (clock-in-out spec)
- FOUND: commit 931c0c1 (pytest tz module)
