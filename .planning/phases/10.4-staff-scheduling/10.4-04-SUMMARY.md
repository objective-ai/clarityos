---
phase: 10.4-staff-scheduling
plan: "04"
subsystem: bff
tags: [bff, staff-scheduling, typescript, csv-export]
dependency_graph:
  requires: [10.4-02]
  provides: [staff-schedule-bff, staff-schedule-types]
  affects: [app/api/staff-schedule/, types/staffSchedule.ts]
tech_stack:
  added: []
  patterns: [proxyToFastAPI, explicit-camelize-transforms, streaming-csv-export]
key_files:
  created:
    - app/api/staff-schedule/[staffId]/schedule/route.ts
    - app/api/staff-schedule/[staffId]/blocked-times/route.ts
    - app/api/staff-schedule/[staffId]/blocked-times/[blockId]/route.ts
    - app/api/staff-schedule/[staffId]/attendance/route.ts
    - app/api/staff-schedule/availability/route.ts
    - app/api/staff-schedule/clock-in/route.ts
    - app/api/staff-schedule/clock-out/route.ts
    - app/api/staff-schedule/clock-status/route.ts
    - app/api/staff-schedule/attendance/route.ts
    - app/api/staff-schedule/attendance/export/route.ts
    - types/staffSchedule.ts
  modified: []
decisions:
  - "Upstream prefix is /api/staff-schedule/ (confirmed from 10.4-02-SUMMARY.md — router registered at /api/staff-schedule not /api/staff to avoid conflict)"
  - "CSV export uses raw fetch with upstream.body streaming — proxyToFastAPI not used (it would await .json() and break streaming)"
  - "Explicit camelize transforms used (not generic recursive) per MEMORY.md feedback_camelizekeys_nested rule"
metrics:
  duration: ~15min
  completed_date: "2026-04-21"
  tasks_completed: 2
  files_changed: 11
---

# Phase 10.4 Plan 04: BFF Proxy Routes + TypeScript Types Summary

10 Next.js BFF route handlers proxying to FastAPI `/api/staff-schedule/*` plus `types/staffSchedule.ts` with camelCase interfaces and explicit snake_case transforms.

## BFF Routes

| Method | BFF Path | Upstream FastAPI Path |
|--------|----------|-----------------------|
| GET, PUT | /api/staff-schedule/[staffId]/schedule/ | /api/staff-schedule/{staffId}/schedule/ |
| GET, POST | /api/staff-schedule/[staffId]/blocked-times/ | /api/staff-schedule/{staffId}/blocked-times/ |
| DELETE | /api/staff-schedule/[staffId]/blocked-times/[blockId]/ | /api/staff-schedule/{staffId}/blocked-times/{blockId}/ |
| GET | /api/staff-schedule/[staffId]/attendance/ | /api/staff-schedule/{staffId}/attendance/ |
| GET | /api/staff-schedule/availability/ | /api/staff-schedule/availability/ |
| POST | /api/staff-schedule/clock-in/ | /api/staff-schedule/clock-in/ |
| POST | /api/staff-schedule/clock-out/ | /api/staff-schedule/clock-out/ |
| GET | /api/staff-schedule/clock-status/ | /api/staff-schedule/clock-status/ |
| GET | /api/staff-schedule/attendance/ | /api/staff-schedule/attendance/ |
| GET | /api/staff-schedule/attendance/export/ | /api/staff-schedule/attendance/export/ (streaming CSV) |

## Auth Scheme

All routes use Supabase `createServerSupabaseClient()` — `proxyToFastAPI` handles auth for 9 routes. The export route replicates the same pattern inline (getUser + getSession) to retain raw stream forwarding without JSON parsing.

## Types Exported (types/staffSchedule.ts)

- `BlockType` — union: "lunch" | "holiday" | "personal" | "other"
- `WeeklyScheduleDay`, `BlockedTime`, `StaffAvailabilityEntry`, `WeeklyAvailabilityResponse`
- `ClockStatus`, `AttendanceRecord`, `AttendanceSummary`
- `camelizeSchedule`, `camelizeBlockedTime`, `camelizeAttendance`, `camelizeClockStatus`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Upstream prefix corrected to /api/staff-schedule/**
- **Found during:** Task 1 read-first (10.4-02-SUMMARY.md)
- **Issue:** Plan template showed upstream paths as `/api/staff/` but 10.4-02 registered the router at `/api/staff-schedule/` to avoid conflict with the existing staff router
- **Fix:** All upstream paths use `/api/staff-schedule/` prefix — matches actual FastAPI router registration
- **Files modified:** All 10 route.ts files
- **Commit:** 4165aca

## Self-Check: PASSED

- app/api/staff-schedule/[staffId]/schedule/route.ts — FOUND
- app/api/staff-schedule/[staffId]/blocked-times/route.ts — FOUND
- app/api/staff-schedule/[staffId]/blocked-times/[blockId]/route.ts — FOUND
- app/api/staff-schedule/[staffId]/attendance/route.ts — FOUND
- app/api/staff-schedule/availability/route.ts — FOUND
- app/api/staff-schedule/clock-in/route.ts — FOUND
- app/api/staff-schedule/clock-out/route.ts — FOUND
- app/api/staff-schedule/clock-status/route.ts — FOUND
- app/api/staff-schedule/attendance/route.ts — FOUND
- app/api/staff-schedule/attendance/export/route.ts — FOUND
- types/staffSchedule.ts — FOUND
- commit 4165aca — FOUND
- commit 2a537b3 — FOUND
