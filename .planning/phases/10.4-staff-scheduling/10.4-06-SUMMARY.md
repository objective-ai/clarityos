---
phase: 10.4-staff-scheduling
plan: "06"
subsystem: topnav
tags: [clock-in, clock-out, staff-scheduling, topnav, ui]
dependency_graph:
  requires: ["10.4-04"]
  provides: ["topnav-clock-button", "topnav-clock-elapsed"]
  affects: ["components/TopNav.tsx", "Plan 07 E2E selectors"]
tech_stack:
  added: []
  patterns: ["pill button", "setInterval elapsed ticker", "optimistic re-fetch on 409"]
key_files:
  created:
    - components/topnav/ClockInButton.tsx
  modified:
    - components/TopNav.tsx
decisions:
  - "staffId sourced from session.user.staffId (camelCase, set by session-hydrator.ts) — not app_metadata"
  - "setInterval (not window.setInterval) used for SSR-safe timer — window.setInterval type is NodeJS.Timeout in Next.js"
  - "ClockInButton inserted before theme-toggle button in TopNav right-hand cluster for visual prominence"
  - "Both data-testids (topnav-clock-button, topnav-clock-elapsed) are non-negotiable — Plan 07 E2E depends on them"
metrics:
  duration: "~8min"
  completed_date: "2026-04-21"
  tasks_completed: 2
  files_changed: 2
---

# Phase 10.4 Plan 06: TopNav ClockInButton Summary

**One-liner:** Self-contained `ClockInButton` pill added to TopNav — fetches clock-status on mount, toggles clock-in/clock-out via POST, shows green `#2DD4BF` + elapsed HH:MM ticker when clocked in; gated by `staffId` presence.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Create ClockInButton component | c5cd8c9 | components/topnav/ClockInButton.tsx |
| 2 | Render ClockInButton in TopNav | 4f6a20d | components/TopNav.tsx |

## Insertion Point in TopNav

`<ClockInButton />` inserted at line 232 (after `<div className="flex items-center gap-2">`) — immediately before the theme toggle `<Button>`. Neighbor components: theme toggle (right), logout (rightmost).

## Session Shape Adjustment

Plan template used `session?.user?.staff_id ?? session?.user?.staffId`. Confirmed `staffId` is camelCase on `AppSession.user` (set by `session-hydrator.ts`). Component uses `session?.user?.staffId` directly.

## Accessibility

- `aria-label`: "Clock in" or "Clock out" (dynamic)
- `title`: "Clock in" or "Clocked in at HH:MM:SS AM/PM" (dynamic, shows exact time when clocked in)
- `disabled` while loading to prevent double-submit

## Data-testids (confirmed present)

```
data-testid="topnav-clock-button"   — on outer <button>
data-testid="topnav-clock-elapsed"  — on <span> inside button (only when clocked in + elapsed computed)
```

Verified with grep exit-0 check before Task 1 commit.

## Deviations from Plan

**1. [Rule 2 - Missing Critical] Used `ReturnType<typeof setInterval>` instead of `number`**
- Found during: Task 1
- Issue: Plan template used `useRef<number | null>` for timer ref; Next.js TypeScript resolves `setInterval` to `NodeJS.Timeout` not `number`, causing potential type mismatch
- Fix: `useRef<ReturnType<typeof setInterval> | null>` — fully portable, no window prefix needed
- Files modified: components/topnav/ClockInButton.tsx
- Commit: c5cd8c9

## Self-Check: PASSED

- FOUND: components/topnav/ClockInButton.tsx
- FOUND: components/TopNav.tsx
- FOUND commit c5cd8c9 (Task 1)
- FOUND commit 4f6a20d (Task 2)
