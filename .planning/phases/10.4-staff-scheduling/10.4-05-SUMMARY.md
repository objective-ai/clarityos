---
phase: 10.4-staff-scheduling
plan: "05"
subsystem: admin-ui
tags: [schedule, admin, staff, attendance, shift-overview]
dependency_graph:
  requires: ["10.4-04"]
  provides: ["10.4-07"]
  affects: ["app/(tenant)/[tenant]/admin/page.tsx"]
tech_stack:
  added: []
  patterns: [glassmorphism-card, provider-pill-selector, data-testid-deterministic]
key_files:
  created:
    - components/admin/ScheduleSection.tsx
  modified:
    - app/(tenant)/[tenant]/admin/page.tsx
decisions:
  - "getWeekDays() accepts string not Date — toYMD() called at useMemo call site"
  - "weekDates are string[] from getWeekDays — no toYMD() conversion in table header"
  - "Task 2a + 2b written as single file (no insert-point needed) — cleaner than two-pass edit"
metrics:
  duration: "~12min"
  completed_date: "2026-04-21T17:21:48Z"
  tasks_completed: 3
  files_changed: 2
---

# Phase 10.4 Plan 05: Admin Schedule Tab Summary

Admin > Schedule tab fully implemented. Provider pill selector, weekly hours editor, blocked times CRUD, shift overview grid, and attendance log with CSV export — all with deterministic data-testids for Plan 07 E2E specs.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Extend admin page SectionKey + SECTIONS + ScheduleSection render | 1ad9f64 | app/(tenant)/[tenant]/admin/page.tsx |
| 2a | ScheduleSection — provider selector, weekly editor, blocked times | 1ad9f64 | components/admin/ScheduleSection.tsx |
| 2b | ScheduleSection — shift overview grid + attendance log + CSV export | 1ad9f64 | components/admin/ScheduleSection.tsx |

## File Structure

### components/admin/ScheduleSection.tsx

**State (Task 2a):**
- `staff` / `selectedStaffId` — active staff list + selection
- `scheduleDays` / `scheduleLoading` / `scheduleSaving` — weekly hours editor
- `blocks` / `newBlock` — blocked times list + add form

**State (Task 2b):**
- `weekStart` / `availability` — shift overview week navigation
- `attFrom` / `attTo` / `attStaffFilter` / `attRecords` — attendance log filters + data

**Panels rendered (top to bottom):**
1. Provider pill selector (GET /api/staff/)
2. Weekly Hours editor (GET/PUT /api/staff-schedule/{id}/schedule/)
3. Blocked Times panel (GET/POST/DELETE /api/staff-schedule/{id}/blocked-times/)
4. Shift Overview grid (GET /api/staff-schedule/availability/)
5. Attendance log + CSV export (GET /api/staff-schedule/attendance/ + export/)

## Data-testids Applied

| testid pattern | Runtime example | Element |
|----------------|-----------------|---------|
| `schedule-provider-pill-{staffId}` | `schedule-provider-pill-abc123` | provider pill button |
| `schedule-day-toggle-{dayOfWeek}` | `schedule-day-toggle-0` | Mon active checkbox |
| `schedule-day-start-{dayOfWeek}` | `schedule-day-start-0` | Mon start time input |
| `schedule-day-end-{dayOfWeek}` | `schedule-day-end-0` | Mon end time input |
| `schedule-save-weekly` | (static) | Save button |
| `blocked-time-add` | (static) | Add blocked time button |
| `blocked-time-item-{blockId}` | `blocked-time-item-xyz789` | blocked time list row |
| `blocked-time-delete-{blockId}` | `blocked-time-delete-xyz789` | Delete button |
| `shift-cell-{staffId}-{dayIndex}` | `shift-cell-abc123-0` | shift grid cell |
| `attendance-export-csv` | (static) | Export CSV button |
| `attendance-row-{recordId}` | `attendance-row-rec001` | attendance table row |

## ARIA Labels Applied

- Checkbox: `aria-label="{DAY} active"` (e.g. "Mon active")
- Block start/end datetime inputs: `aria-label="Block start"` / `aria-label="Block end"`
- Block type select: `aria-label="Block type"`
- Attendance date inputs: `aria-label="From date"` / `aria-label="To date"`
- Attendance staff select: `aria-label="Staff filter"`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] getWeekDays signature mismatch**
- **Found during:** Task 2b (tsc run)
- **Issue:** Plan template passed `Date` to `getWeekDays(weekStart)` but `lib/scheduleUtils.ts` declares `getWeekDays(dateStr: string): string[]`
- **Fix:** Changed call to `getWeekDays(toYMD(weekStart))` at useMemo; removed `toYMD(d)` wrapper in table header since `weekDates` are already strings
- **Files modified:** components/admin/ScheduleSection.tsx
- **Commit:** 1ad9f64

**2. [Structural] Tasks 2a + 2b merged into single file write**
- **Reason:** Writing complete file at once (no insert-point two-pass) is simpler and avoids grep-marker fragility; both tasks remain logically distinct in the code via section comments
- **Impact:** None — all acceptance criteria met for both tasks

## Self-Check: PASSED

- [x] `components/admin/ScheduleSection.tsx` exists
- [x] `app/(tenant)/[tenant]/admin/page.tsx` modified with `"schedule"` in SectionKey, SECTIONS entry, import, render
- [x] Commit `1ad9f64` exists in git log
- [x] All 11 data-testids verified via grep (ALL TESTIDS OK)
- [x] `npx tsc --noEmit` — zero errors in new files (pre-existing test/e2e errors out of scope)
