---
phase: 10.2-revamp-schedule-booking-page
plan: "07"
subsystem: schedule
tags: [bug-fix, uat-gap-closure, schedule, drawer]
dependency_graph:
  requires: []
  provides: [visible-view-tabs, drawer-hidden-on-load, drawer-close-controls]
  affects: [schedule-page, appointment-detail-drawer]
tech_stack:
  added: []
  patterns: [explicit-tailwind-opacity-classes, conditional-render-gate]
key_files:
  modified:
    - app/(tenant)/[tenant]/schedule/page.tsx
    - components/schedule/AppointmentDetailDrawer.tsx
decisions:
  - "Explicit Tailwind opacity classes (text-white/60) used over CSS variables for tab text — CSS variables have insufficient contrast on glassmorphism, matches Phase 10.2-06 pattern"
  - "Early return null when !open && !appointment prevents SSR hydration flash — more reliable than CSS-only translate"
  - "Always-visible X button placed outside appt ternary so it renders even in empty/loading state"
metrics:
  duration: "~8min"
  completed_date: "2026-04-03"
  tasks: 2
  files_modified: 2
---

# Phase 10.2 Plan 07: UAT Gap Closure — Tab Visibility + Drawer Bugs Summary

**One-liner:** Fixed 3 UAT-reported bugs: invisible view mode tabs (CSS variable contrast), drawer visible on page load (SSR flash), and missing close controls in drawer empty state.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Fix view mode tab visibility | 2ff3ee9 | app/(tenant)/[tenant]/schedule/page.tsx |
| 2 | Fix detail drawer — conditional render gate + close controls | ee4d92e | components/schedule/AppointmentDetailDrawer.tsx |

## What Was Built

**Task 1 — View toggle tab visibility:**
- Replaced `text-[var(--text-muted)]` with `text-white/60` for inactive tab text
- Replaced `border-[var(--border-default)]` with `border-white/20` on container
- Added `hover:text-white/80 hover:bg-white/5` for hover state
- Active tab (`bg-[var(--accent)]`) unchanged — was already visible

**Task 2 — Drawer bugs (3 bugs in one file):**
- Bug A: Added `if (!open && !appt) return null` early return to prevent drawer rendering on page load; also added `pointer-events-none` to panel when closed
- Bug B: Moved X close button outside `appt ? (...)` ternary as absolutely-positioned element with `type="button"`, `aria-label`, always accessible; removed duplicate from header
- Bug C: ESC handler was already correct — was masked by Bug A (drawer shouldn't be visible when `open` is false)

## Verification

- `npx tsc --noEmit`: Pre-existing e2e test errors only; no errors in modified files
- UAT tests 2, 4, 5 should now pass on re-test
- UAT tests 3, 6-11 unblocked (were skipped due to tabs invisible or drawer blocking)

## Deviations from Plan

**1. [Rule 2 - Missing Critical] Added type="button" to always-visible close button**
- Found during: Task 2 (IDE diagnostic hint)
- Issue: Button without `type` attribute defaults to `type="submit"` inside forms — can cause unintended form submission
- Fix: Added `type="button"` to the always-visible close button
- Files modified: components/schedule/AppointmentDetailDrawer.tsx
- Commit: ee4d92e (included in task commit)

## Self-Check: PASSED

- `app/(tenant)/[tenant]/schedule/page.tsx` — exists, modified
- `components/schedule/AppointmentDetailDrawer.tsx` — exists, modified
- Commit 2ff3ee9 — exists
- Commit ee4d92e — exists
