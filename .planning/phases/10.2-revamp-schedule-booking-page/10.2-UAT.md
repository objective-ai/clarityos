---
status: diagnosed
phase: 10.2-revamp-schedule-booking-page
source: [10.2-01-SUMMARY.md, 10.2-02-SUMMARY.md, 10.2-03-SUMMARY.md, 10.2-04-SUMMARY.md, 10.2-05-SUMMARY.md, 10.2-06-SUMMARY.md]
started: 2026-04-03T12:00:00Z
updated: 2026-04-03T14:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running servers. Restart from scratch via `bash scripts/dev.sh ensure-api`. Both servers boot, migration runs, `check-api` returns healthy, schedule page loads.
result: pass

### 2. Schedule Page — 5 View Mode Tabs
expected: Navigate to the schedule page. You see 5 view mode tabs: List, Timeline, Clinic, Flow, Week. Clicking each tab switches the view below. The selected tab is visually highlighted.
result: issue
reported: "5 view mode tabs are not visible on the schedule page at all"
severity: major

### 3. WeekStrip Navigation
expected: A horizontal week strip shows 7 day cells (Mon–Sun or Sun–Sat). The current day is highlighted. Clicking a different day selects it. Chevron arrows on either side let you navigate to previous/next week.
result: skipped
reason: Sidebar blocking interaction, unable to verify

### 4. Appointment Detail Drawer — Open on Card Click
expected: In List view, click any appointment card. A 480px drawer slides in from the right showing: patient name, appointment type, colored status badge, wait time badge, details grid (date, time, duration, provider, type, chief complaint, insurance, copay), and action buttons at the bottom.
result: issue
reported: "it shows on page load — sidebar with 'No appointment selected' appears immediately without clicking any card"
severity: major

### 5. Detail Drawer — Close Interactions
expected: With the detail drawer open: (1) pressing ESC closes it, (2) clicking the dark backdrop closes it, (3) clicking the X button closes it. The drawer slides out smoothly.
result: issue
reported: "cant close sidebar — ESC, backdrop click, and X button do not close it"
severity: blocker

### 6. Detail Drawer — Status-Aware Actions
expected: The action buttons at the bottom of the detail drawer change based on appointment status. For a "scheduled" appointment you see "Check In". For "arrived" you see "Start Pre-Test". Reschedule and Cancel (red) buttons are always present.
result: skipped
reason: Detail drawer not functioning correctly

### 7. Booking Drawer — Slot Picker Grid
expected: Click the Book/New Appointment button on the schedule page. A 480px drawer slides in from the right with a 4-column grid of 30-minute time slots (8:00 AM to 5:30 PM). Available slots are clickable with accent hover. Occupied slots are greyed out with a tooltip showing who has that slot.
result: skipped
reason: Sidebar blocking interaction

### 8. Book an Appointment via Drawer
expected: In the booking drawer: search for a patient by name (results appear in dropdown), select a patient, pick an available slot (it highlights), fill required fields, and submit. The appointment is created, the drawer closes, and the new appointment appears in the schedule list.
result: skipped
reason: Sidebar blocking interaction

### 9. FlowBoard — Kanban Columns
expected: Switch to Flow view. You see 4 Kanban columns: Waiting, Pre-Test, In Exam, Done. Appointments are sorted into columns based on their status. Each card shows patient name and wait time (amber >15min, red >30min). An "Upcoming" strip above the columns shows scheduled/confirmed appointments.
result: skipped
reason: View mode tabs not visible, cannot switch to Flow view

### 10. FlowBoard — Status Transition Buttons
expected: On FlowBoard cards: an "arrived" patient card shows a "Start Pre-Test" button. Clicking it moves the card to the Pre-Test column. An "in_pretest" card shows "Start Exam". Empty columns show "{Column} is clear" message.
result: skipped
reason: View mode tabs not visible, cannot switch to Flow view

### 11. Week View — 7-Day Grid
expected: Switch to Week view. You see a 7-day time-aligned grid (Google Calendar style). Time axis runs 8 AM to 6 PM on the left. Appointment blocks are positioned at their correct time and day. Today's column header is highlighted in accent color. A red "now" line appears on today's column at the current time.
result: skipped
reason: View mode tabs not visible, cannot switch to Week view

### 12. Public Booking — Light Theme
expected: Navigate to /book/sunview (or your clinic slug) in a new tab. The page renders with a light theme (white/gray background, dark text). No dark glassmorphism leakage from the internal app. The accent color (#2DD4BF) is used for buttons and selected states.
result: pass

### 13. Public Booking — Complete Wizard Flow
expected: On the public booking page, step through the 5-step wizard: (1) Select visit type, (2) Select provider, (3) Pick a date from the 7-day availability strip and select a time slot, (4) Fill patient info (first name, last name, phone required), (5) Confirm. After confirming, you see a "You're booked!" success screen.
result: issue
reported: "error on booking — confirm step shows [object Object],[object Object] error banner, API returns error objects rendered as strings"
severity: major

## Summary

total: 13
passed: 2
issues: 4
pending: 0
skipped: 7

## Gaps

- truth: "Schedule page shows 5 view mode tabs (List, Timeline, Clinic, Flow, Week)"
  status: failed
  reason: "User reported: 5 view mode tabs are not visible on the schedule page at all"
  severity: major
  test: 2
  root_cause: "Tabs exist in JSX (page.tsx lines 280-294) but CSS variables --text-muted and --border-default may have insufficient contrast against glassmorphism background, making tabs invisible"
  artifacts:
    - path: "app/(tenant)/[tenant]/schedule/page.tsx"
      issue: "View toggle buttons use text-[var(--text-muted)] and border-[var(--border-default)] which may be invisible"
  missing:
    - "Verify CSS variable contrast; may need explicit Tailwind colors or opacity adjustment"
  debug_session: ""

- truth: "Detail drawer only opens when user clicks an appointment card"
  status: failed
  reason: "User reported: sidebar with 'No appointment selected' appears immediately on page load without clicking any card"
  severity: major
  test: 4
  root_cause: "AppointmentDetailDrawer.tsx has no render gate — entire drawer always mounted in DOM, relies on CSS translate-x-full to hide. During SSR hydration transform may not apply on first paint, showing panel at full visibility"
  artifacts:
    - path: "components/schedule/AppointmentDetailDrawer.tsx"
      issue: "Lines 100-115: panel uses CSS-only hide, no conditional render, no pointer-events-none when closed"
  missing:
    - "Add early return (if (!open) return null) or pointer-events-none when !open"
  debug_session: ".planning/debug/detail-drawer-visible-on-load.md"

- truth: "Detail drawer can be closed via ESC, backdrop click, or X button"
  status: failed
  reason: "User reported: cant close sidebar — ESC, backdrop click, and X button do not close it"
  severity: blocker
  test: 5
  root_cause: "X button is inside the appt ternary (truthy branch only) — empty state (lines 303-308) has no dismiss affordances. ESC handler gated on open (false during this bug). Backdrop has pointer-events-none."
  artifacts:
    - path: "components/schedule/AppointmentDetailDrawer.tsx"
      issue: "Lines 303-308: empty state has no X button; line 66: ESC gated on open"
  missing:
    - "Move X button outside appt ternary so it's always accessible"
  debug_session: ".planning/debug/detail-drawer-visible-on-load.md"

- truth: "Public booking wizard completes with 'You're booked!' success screen"
  status: failed
  reason: "User reported: confirm step shows [object Object],[object Object] error banner, API returns error objects rendered as strings"
  severity: major
  test: 13
  root_cause: "Two bugs: (A) dob and sex sent as undefined but required by PublicBookingRequest schema — FastAPI returns 422 with detail array of validation objects. (B) Error handler (line 537) does new Error(data.detail) which calls .toString() on array, producing [object Object],[object Object]"
  artifacts:
    - path: "app/book/[slug]/page.tsx"
      issue: "Lines 518-519: dob/sex sent as undefined; lines 537-538: error handler assumes detail is string"
    - path: "backend/schemas/public_booking.py"
      issue: "Lines 78-82: dob and sex are required with no defaults"
  missing:
    - "Handle array detail by mapping .msg strings"
    - "Either require dob/sex in UI validation or make them optional in Pydantic schema"
  debug_session: ".planning/debug/booking-object-object-error.md"
