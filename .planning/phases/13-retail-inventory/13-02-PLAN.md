---
id: 13-02
phase: 13-retail-inventory
plan: 02
type: execute
wave: 1
depends_on: ["13-00"]
files_modified:
  - backend/core/permissions.py
  - backend/core/entitlements.py
  - lib/entitlements.ts
autonomous: true
requirements: [INV-14, INV-19]
must_haves:
  truths:
    - "5 new ClinicalAction values exist with correct PERMISSION_MATRIX rows"
    - "retail_pos entitlement key exists in BOTH Python Entitlement enum and TS Entitlement const"
    - "retail_pos is NOT added to PLAN_FEATURES Core/Plus/Premium arrays in either Python or TS"
    - "ENTITLEMENT_META has retail_pos entry with plan: 'Add-on'"
  artifacts:
    - path: backend/core/permissions.py
      provides: "5 new ClinicalAction enum values + matrix rows"
      contains: "VIEW_INVENTORY = \"view_inventory\""
    - path: backend/core/entitlements.py
      provides: "RETAIL_POS entitlement key (BE)"
      contains: "RETAIL_POS = \"retail_pos\""
    - path: lib/entitlements.ts
      provides: "RETAIL_POS entitlement key (FE) + ENTITLEMENT_META entry"
      contains: "RETAIL_POS: \"retail_pos\""
  key_links:
    - from: backend/core/entitlements.py
      to: backend/core/entitlements.py PLAN_FEATURES dict
      via: "add-on NOT in plan tiers"
      pattern: "(?!.*Entitlement\\.RETAIL_POS).*PLAN_FEATURES"
    - from: lib/entitlements.ts
      to: lib/entitlements.ts PLAN_FEATURES const
      via: "add-on NOT in plan tiers"
      pattern: "(?!.*RETAIL_POS).*PLAN_FEATURES"
---

<objective>
Wire the 5 new `ClinicalAction` permission values (with PERMISSION_MATRIX rows) and the `retail_pos` entitlement key on BOTH the Python and TypeScript sides as a single bundled add-on. Critical discriminator from CONTEXT §H: `retail_pos` is **NOT** added to `PLAN_FEATURES["Core"]`, `["Plus"]`, or `["Premium"]` — it is purchased separately, mirroring the MESSAGING precedent BUT with this single twist. The `ENTITLEMENT_META` entry uses `plan: "Add-on"`.

Purpose: Wave 2 routes will call `require_permission(ClinicalAction.MANAGE_INVENTORY)` and `require_entitlement("retail_pos")`. Wave 3 sidebar/UI will call `has(Entitlement.RETAIL_POS)`. This plan lands those symbols.

Output: Permission gating returns 403 for non-{ADMIN,OWNER} on MANAGE_INVENTORY routes; entitlement check is wireable; sidebar `requiredEntitlement: Entitlement.RETAIL_POS` lookup works.
</objective>

<execution_context>
@C:/Users/duytr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/duytr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/13-retail-inventory/13-CONTEXT.md
@.planning/phases/13-retail-inventory/13-RESEARCH.md
@.planning/phases/13-retail-inventory/13-00-SUMMARY.md
@.claude/rules/clinical-safety.md
</context>

<interfaces>
<!-- Donor patterns verified in 13-RESEARCH.md -->

`backend/core/permissions.py:26-82` — ClinicalAction enum.
`backend/core/permissions.py:94-151` — PERMISSION_MATRIX dict mapping action → set of role aliases (`_D`, `_T`, `_R`, `_A`, `_O`).
`backend/core/permissions.py:159` — `require_permission(action)` factory.

`backend/core/entitlements.py:24-37` — Entitlement StrEnum (MESSAGING is the most recent precedent).
`backend/core/entitlements.py` PLAN_FEATURES dict — `{"Core": [...], "Plus": [...], "Premium": [...]}`. **Do NOT add RETAIL_POS to any of these arrays.**
`backend/core/entitlements.py:76` — `require_entitlement(key)` factory.

`lib/entitlements.ts:25-49` — Entitlement const object.
`lib/entitlements.ts:90-155` — ENTITLEMENT_META dict with `label`, `description`, `plan` fields.
`lib/entitlements.ts` PLAN_FEATURES const — same shape; **DO NOT add RETAIL_POS**.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend ClinicalAction enum + PERMISSION_MATRIX</name>
  <files>backend/core/permissions.py</files>

  <read_first>
    backend/core/permissions.py (read full file — locate ClinicalAction enum lines 26-82 and PERMISSION_MATRIX dict lines 94-151; identify role-alias short names `_D _T _R _A _O`),
    .planning/phases/13-retail-inventory/13-CONTEXT.md §F (lines 70-75 — exact role assignments per action),
    .planning/phases/13-retail-inventory/13-RESEARCH.md (lines 477-495 — verbatim diff)
  </read_first>

  <action>
Edit `backend/core/permissions.py` in two places.

**Location 1: extend `ClinicalAction` StrEnum** — add 5 new values at the end of the enum body (preserving alphabetic / categorical grouping if the file has comments separating sections; otherwise append at end). Match existing string-value style:
```python
    # Phase 13 — Retail Inventory & Optical Orders
    VIEW_INVENTORY = "view_inventory"
    MANAGE_INVENTORY = "manage_inventory"
    CREATE_OPTICAL_ORDER = "create_optical_order"
    VIEW_OPTICAL_ORDER = "view_optical_order"
    CANCEL_OPTICAL_ORDER = "cancel_optical_order"
```

**Location 2: extend `PERMISSION_MATRIX` dict** — add 5 rows at the end of the dict (just before the closing brace). Use the file's existing role aliases — locate them at the top (typically `_D = Role.DOCTOR`, `_T = Role.TECHNICIAN`, `_R = Role.RECEPTIONIST`, `_A = Role.ADMIN`, `_O = Role.OWNER`). Use the actual aliases as defined in this file:
```python
    ClinicalAction.VIEW_INVENTORY:        {_D, _T, _R, _A, _O},
    ClinicalAction.MANAGE_INVENTORY:      {_A, _O},
    ClinicalAction.CREATE_OPTICAL_ORDER:  {_T, _R, _A, _O},
    ClinicalAction.VIEW_OPTICAL_ORDER:    {_D, _T, _R, _A, _O},
    ClinicalAction.CANCEL_OPTICAL_ORDER:  {_A, _O},
```

If the file uses different short names (e.g., spelled-out `Role.OWNER`), use whatever is consistent with the rest of the matrix. Do NOT alter existing rows.
  </action>

  <verify>
    <automated>cd backend && python -c "from backend.core.permissions import ClinicalAction, PERMISSION_MATRIX, Role; assert ClinicalAction.MANAGE_INVENTORY.value == 'manage_inventory'; assert PERMISSION_MATRIX[ClinicalAction.MANAGE_INVENTORY] == {Role.ADMIN, Role.OWNER}; assert Role.DOCTOR not in PERMISSION_MATRIX[ClinicalAction.MANAGE_INVENTORY]; assert Role.DOCTOR in PERMISSION_MATRIX[ClinicalAction.VIEW_INVENTORY]; print('OK')"</automated>
  </verify>

  <acceptance_criteria>
    - File `backend/core/permissions.py` contains the literal strings: `VIEW_INVENTORY = "view_inventory"`, `MANAGE_INVENTORY = "manage_inventory"`, `CREATE_OPTICAL_ORDER = "create_optical_order"`, `VIEW_OPTICAL_ORDER = "view_optical_order"`, `CANCEL_OPTICAL_ORDER = "cancel_optical_order"`
    - File contains all 5 new PERMISSION_MATRIX rows (`ClinicalAction.VIEW_INVENTORY:`, `ClinicalAction.MANAGE_INVENTORY:`, `ClinicalAction.CREATE_OPTICAL_ORDER:`, `ClinicalAction.VIEW_OPTICAL_ORDER:`, `ClinicalAction.CANCEL_OPTICAL_ORDER:`)
    - `MANAGE_INVENTORY` matrix entry contains only OWNER and ADMIN (no DOCTOR/TECHNICIAN/RECEPTIONIST)
    - `CANCEL_OPTICAL_ORDER` matrix entry contains only OWNER and ADMIN
    - `VIEW_INVENTORY` matrix entry contains all five roles
    - `cd backend && python -c "from backend.core.permissions import ClinicalAction, PERMISSION_MATRIX, Role; assert PERMISSION_MATRIX[ClinicalAction.MANAGE_INVENTORY] == {Role.ADMIN, Role.OWNER}"` exits 0
    - Wave 0 stub `pytest backend/tests/test_inventory_permissions.py::test_manage_inventory_owner_admin_only --collect-only -q` collects without import errors
  </acceptance_criteria>

  <done>5 new ClinicalAction enum values exist; PERMISSION_MATRIX has all 5 corresponding rows with correct role assignments per CONTEXT §F.</done>
</task>

<task type="auto">
  <name>Task 2: Wire retail_pos entitlement (Python + TypeScript) — NOT in PLAN_FEATURES</name>
  <files>
    backend/core/entitlements.py,
    lib/entitlements.ts
  </files>

  <read_first>
    backend/core/entitlements.py (read entire file — locate Entitlement enum and PLAN_FEATURES dict; MESSAGING is the most recent precedent at line ~36-37),
    lib/entitlements.ts (read entire file — Entitlement const at lines 25-49, ENTITLEMENT_META at lines 90-155, PLAN_FEATURES const),
    .planning/phases/13-retail-inventory/13-CONTEXT.md §H (lines 79-87 — entitlement gating spec),
    .planning/phases/13-retail-inventory/13-RESEARCH.md (lines 439-474 — exact entitlement-pair pattern + the explicit "DO NOT add to PLAN_FEATURES" note)
  </read_first>

  <action>
**Step A — `backend/core/entitlements.py`:**

Add ONE new value to the `Entitlement` StrEnum (alongside MESSAGING):
```python
class Entitlement(StrEnum):
    # ... existing values, including:
    MESSAGING = "messaging"
    RETAIL_POS = "retail_pos"   # ← NEW (Phase 13 + Phase 15 add-on, $150/mo)
```

**DO NOT** modify the `PLAN_FEATURES` dict. Specifically: open the dict, scan all three lists (`"Core"`, `"Plus"`, `"Premium"`), and confirm `Entitlement.RETAIL_POS` is absent. The add-on is purchased separately at the billing layer (out of scope for Phase 13).

**Step B — `lib/entitlements.ts`:**

1. Add to the `Entitlement` const object (alongside MESSAGING):
```typescript
export const Entitlement = {
  // ... existing ...
  MESSAGING: "messaging" as const,
  RETAIL_POS: "retail_pos" as const,   // ← NEW
} satisfies Record<string, EntitlementKey>;
```

2. Add to `ENTITLEMENT_META` (insert as a new key at the end of the meta dict):
```typescript
export const ENTITLEMENT_META: Record<EntitlementKey, EntitlementMetadata> = {
  // ... existing ...
  retail_pos: {
    label: "Retail & POS",
    description: "Inventory catalog, optical orders, and point-of-sale checkout.",
    plan: "Add-on",
  },
};
```

3. Update the `EntitlementKey` type if it's a string-literal union — append `"retail_pos"` to the union:
```typescript
export type EntitlementKey = "scheduling" | "messaging" | "retail_pos" | ...;
```
(If the type is derived from `keyof typeof Entitlement` or similar, no edit needed — verify by reading the existing type definition.)

4. **DO NOT** add RETAIL_POS to the `PLAN_FEATURES` const. Open the const, scan all three plan arrays (`Core`, `Plus`, `Premium`), and confirm absence.

**Sanity check after edits:** open both files and grep for `RETAIL_POS` — expect exactly:
- `backend/core/entitlements.py`: 1 occurrence (the enum value definition).
- `lib/entitlements.ts`: 2 occurrences (the const and the type union, OR just the const if the type is inferred), plus 1 occurrence of the lowercase string `retail_pos` in ENTITLEMENT_META.

If you see RETAIL_POS appearing inside any PLAN_FEATURES context, REMOVE it.
  </action>

  <verify>
    <automated>cd backend && python -c "from backend.core.entitlements import Entitlement, PLAN_FEATURES; assert Entitlement.RETAIL_POS.value == 'retail_pos'; assert Entitlement.RETAIL_POS not in PLAN_FEATURES.get('Core', []); assert Entitlement.RETAIL_POS not in PLAN_FEATURES.get('Plus', []); assert Entitlement.RETAIL_POS not in PLAN_FEATURES.get('Premium', []); print('BE OK')" && cd .. && npx tsc --noEmit lib/entitlements.ts 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - File `backend/core/entitlements.py` contains the literal string `RETAIL_POS = "retail_pos"`
    - In `backend/core/entitlements.py`, the `PLAN_FEATURES` dict's `"Core"`, `"Plus"`, and `"Premium"` arrays do NOT contain `Entitlement.RETAIL_POS` (verified by Python assertion in verify command)
    - File `lib/entitlements.ts` contains the literal string `RETAIL_POS: "retail_pos"`
    - File `lib/entitlements.ts` contains the literal strings `label: "Retail & POS"` AND `plan: "Add-on"` (the ENTITLEMENT_META entry)
    - In `lib/entitlements.ts`, `PLAN_FEATURES` const Core/Plus/Premium arrays do NOT contain `Entitlement.RETAIL_POS` or the literal `"retail_pos"` string within plan-tier definitions (grep `retail_pos` in the file should NOT show it inside `PLAN_FEATURES` braces; only inside `Entitlement` const + `ENTITLEMENT_META`)
    - `cd backend && python -c "from backend.core.entitlements import Entitlement, PLAN_FEATURES; assert Entitlement.RETAIL_POS not in PLAN_FEATURES['Core'] and Entitlement.RETAIL_POS not in PLAN_FEATURES['Plus'] and Entitlement.RETAIL_POS not in PLAN_FEATURES['Premium']"` exits 0
    - `npx tsc --noEmit` exits 0 (no TS errors introduced)
  </acceptance_criteria>

  <done>retail_pos entitlement exists on BE+FE, ENTITLEMENT_META documents it as Add-on, and PLAN_FEATURES is verifiably untouched on both sides.</done>
</task>

</tasks>

<verification>
- `cd backend && python -c "from backend.core.permissions import ClinicalAction, PERMISSION_MATRIX, Role; from backend.core.entitlements import Entitlement, PLAN_FEATURES; assert PERMISSION_MATRIX[ClinicalAction.MANAGE_INVENTORY] == {Role.ADMIN, Role.OWNER}; assert all(Entitlement.RETAIL_POS not in PLAN_FEATURES[t] for t in ('Core','Plus','Premium')); print('OK')"`
- `npx tsc --noEmit` exits 0
- Wave 0 stub `pytest backend/tests/test_inventory_permissions.py --collect-only` no longer reports importorskip on `backend.core.permissions`
</verification>

<success_criteria>
- INV-19 satisfied: 5 new ClinicalAction values + correct PERMISSION_MATRIX rows
- INV-14 satisfied: retail_pos entitlement on BE+FE; NOT in PLAN_FEATURES
</success_criteria>

<output>
After completion, create `.planning/phases/13-retail-inventory/13-02-SUMMARY.md`
</output>
