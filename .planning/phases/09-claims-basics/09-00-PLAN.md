---
phase: 09-claims-basics
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/lib/feeService.test.ts
  - tests/unit/store/payerStore.test.ts
  - backend/tests/test_fee_service.py
  - tests/e2e/verify-payers-admin.js
  - tests/e2e/verify-patient-insurance.js
  - tests/e2e/verify-patient-billing.js
autonomous: true
requirements:
  - INS-01
  - INS-02
  - INS-03
  - INS-04
  - INS-05
  - INS-06
  - INS-07

must_haves:
  truths:
    - "All Wave 0 test stubs exist on disk before implementation begins"
    - "Automated verify commands reference real file paths (not placeholders)"
    - "Vitest can discover the new test files without configuration changes"
    - "pytest can discover the new Python test file without configuration changes"
  artifacts:
    - path: "tests/unit/lib/feeService.test.ts"
      provides: "Stub test for fee resolution TS logic (INS-02)"
    - path: "tests/unit/store/payerStore.test.ts"
      provides: "Stub test for payerStore state (INS-05)"
    - path: "backend/tests/test_fee_service.py"
      provides: "Stub test for Python fee resolution (INS-05)"
    - path: "tests/e2e/verify-payers-admin.js"
      provides: "E2E smoke stub for admin Payers tab (INS-03)"
    - path: "tests/e2e/verify-patient-insurance.js"
      provides: "E2E smoke stub for patient Insurance tab (INS-04)"
    - path: "tests/e2e/verify-patient-billing.js"
      provides: "E2E smoke stub for patient Billing tab (INS-07)"
  key_links:
    - from: "tests/unit/lib/feeService.test.ts"
      to: "src/lib/feeService.ts (to be created in plan 09-03)"
      via: "vitest import"
      pattern: "import.*feeService"
    - from: "backend/tests/test_fee_service.py"
      to: "backend/services/fee_service.py (to be created in plan 09-02)"
      via: "pytest import"
      pattern: "from services.fee_service import"
---

<objective>
Create Wave 0 test scaffolding required by the Nyquist rule before any implementation begins.

Purpose: Subsequent plans reference these test files in their `<verify>` blocks. Without them the executor would have MISSING automated verify stubs, breaking the feedback sampling contract from VALIDATION.md.
Output: 6 test stub files — 2 Vitest (TS), 1 pytest (Python), 3 Playwright E2E smoke stubs.
</objective>

<execution_context>
@C:/Users/duytr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/duytr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-claims-basics/09-CONTEXT.md
@.planning/phases/09-claims-basics/09-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Vitest unit test stubs (TS side)</name>
  <files>tests/unit/lib/feeService.test.ts, tests/unit/store/payerStore.test.ts</files>
  <behavior>
    - feeService.test.ts: test that resolveFee(cptCode, payerRates, baseRates) returns payer rate when payer has override; returns base rate when payer has no override; returns 0.00 and fee_source "base_rate" when neither has entry
    - payerStore.test.ts: test initial state has empty payers array; test that setPatientInsurance sets primary and secondary separately
  </behavior>
  <action>
    Create tests/unit/lib/feeService.test.ts:
    ```typescript
    import { describe, it, expect } from "vitest";

    // Stub — implementation created in plan 09-02 (billing_service.ts extension)
    // These tests will FAIL (red) until feeService.ts is created
    describe.skip("resolveFee (stub — fails until plan 09-02)", () => {
      it("returns payer rate when payer has CPT override", () => {
        expect(true).toBe(true); // placeholder
      });
      it("falls back to base rate when payer has no override", () => {
        expect(true).toBe(true);
      });
      it("returns 0.00 base_rate when neither payer nor base has entry", () => {
        expect(true).toBe(true);
      });
    });
    ```

    Create tests/unit/store/payerStore.test.ts:
    ```typescript
    import { describe, it, expect } from "vitest";

    // Stub — implementation created in plan 09-04
    describe.skip("payerStore (stub — fails until plan 09-04)", () => {
      it("initial state has empty payers array", () => {
        expect(true).toBe(true);
      });
      it("setPatientInsurance sets primary separately from secondary", () => {
        expect(true).toBe(true);
      });
    });
    ```

    Both files use `describe.skip` so vitest passes (not fails) the suite while marking them as pending. They will be unskipped and fleshed out in their respective implementation plans.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/lib/feeService.test.ts tests/unit/store/payerStore.test.ts --reporter=verbose 2>&1 | head -20</automated>
  </verify>
  <done>Both files discoverable by vitest with 0 failing tests (stubs are skipped)</done>
</task>

<task type="auto">
  <name>Task 2: Create pytest stub + E2E smoke stubs</name>
  <files>backend/tests/test_fee_service.py, tests/e2e/verify-payers-admin.js, tests/e2e/verify-patient-insurance.js, tests/e2e/verify-patient-billing.js</files>
  <action>
    Create backend/tests/test_fee_service.py:
    ```python
    """
    Fee service unit tests — Wave 0 stub.
    Implementation in plan 09-02 (fee_service.py creation).
    Tests are marked skip until fee_service.py exists.
    """
    import pytest


    @pytest.mark.skip(reason="stub — fee_service.py created in plan 09-02")
    def test_resolve_fee_returns_payer_rate():
        """resolve_line_item_fee returns payer-specific rate when payer override exists."""
        pass


    @pytest.mark.skip(reason="stub — fee_service.py created in plan 09-02")
    def test_resolve_fee_fallback():
        """resolve_line_item_fee falls back to base catalog rate when no payer override."""
        pass


    @pytest.mark.skip(reason="stub — fee_service.py created in plan 09-02")
    def test_resolve_fee_returns_zero_when_missing():
        """resolve_line_item_fee returns Decimal('0.00') when neither payer nor base has entry."""
        pass
    ```

    Create tests/e2e/verify-payers-admin.js using the project playwright-cli pattern (require helper utils, loginOrRestore, navigate to admin, click Payers tab, assert table visible):
    ```javascript
    // E2E smoke — Admin Payers tab (INS-03)
    // Wave 0 stub: always passes; real assertions added in plan 09-04
    const { chromium } = require("playwright");
    const { loginOrRestore } = require("./helpers/test-utils");

    (async () => {
      const browser = await chromium.launch();
      const context = await browser.newContext();
      const page = await context.newPage();
      await loginOrRestore(page, context);
      console.log("STUB PASS — verify-payers-admin: real assertions added in plan 09-04");
      await browser.close();
    })();
    ```

    Create tests/e2e/verify-patient-insurance.js — same structure, stub pass, note "real assertions added in plan 09-05".
    Create tests/e2e/verify-patient-billing.js — same structure, stub pass, note "real assertions added in plan 09-05".
  </action>
  <verify>
    <automated>cd C:/Users/duytr/Projects/clarityos && python -m pytest backend/tests/test_fee_service.py -v 2>&1 | tail -10</automated>
  </verify>
  <done>pytest discovers test_fee_service.py with 3 tests marked SKIPPED (0 failed); 3 E2E js stubs exist on disk</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/lib/feeService.test.ts tests/unit/store/payerStore.test.ts` passes (stubs skipped)
- `python -m pytest backend/tests/test_fee_service.py -v` shows 3 SKIPPED, 0 FAILED
- All 6 stub files exist at their declared paths
</verification>

<success_criteria>
Wave 0 complete when all 6 stub files exist and automated commands above return 0 failures.
</success_criteria>

<output>
After completion, create `.planning/phases/09-claims-basics/09-00-SUMMARY.md`
</output>
