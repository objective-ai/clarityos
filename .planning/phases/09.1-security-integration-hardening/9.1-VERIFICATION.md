---
phase: 9.1-security-integration-hardening
verified: 2026-03-26T00:00:00Z
status: passed
score: 8/8 must-haves verified
re_verification: false
---

# Phase 9.1: Security + Integration Hardening — Verification Report

**Phase Goal:** Fix auth hardening gaps (api-client bypass, middleware blanket exclusion) and wire fee catalog into billing workflow
**Verified:** 2026-03-26
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | fetchPatientInsurance sends Supabase JWT bearer token | VERIFIED | `getAuthHeaders()` called on line 137 of `lib/api-client.ts` before `fetch(…/insurance)` |
| 2 | fetchSuperbillPdfBlob sends Supabase JWT bearer token | VERIFIED | `getAuthHeaders()` called on line 154 of `lib/api-client.ts` before `fetch(…/superbill/pdf)` |
| 3 | Middleware denies unauthenticated requests to non-public /api/* routes | VERIFIED | Blanket `startsWith("/api")` exclusion is gone; only `/api/public/` and `/api/address/` are exempt |
| 4 | Public API routes (intake, booking, health) remain accessible without auth | VERIFIED | `pathname.startsWith("/api/public/")` and `pathname.startsWith("/api/address/")` preserved in isPublicRoute |
| 5 | Payer selection loads payer-specific fee rates for line items | VERIFIED | `fetchPayerFeeSchedule` called in `handleConfirmPayer` and `handlePayerChange`; result stored in `payerFeeMap` state |
| 6 | Line item fees fall back to CPT_CATALOG defaultFee when no payer schedule exists | VERIFIED | `payerFee ?? entry.defaultFee` pattern present in both `handleAddCpt` and `CptAddDropdown.handleSelect` |
| 7 | Adding a new CPT code uses payer rate if available | VERIFIED | `handleAddCpt` reads `payerFeeMap.get(entry.code)` before falling back to `entry.defaultFee` |
| 8 | CptAddDropdown displays payer-specific fees when a payer is selected | VERIFIED | `payerFeeMap` prop wired to `CptAddDropdown`; dropdown rendering uses `payerFeeMap?.has(entry.code)` to toggle payer vs base rate display |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/api-client.ts` | Auth headers on fetchPatientInsurance + fetchSuperbillPdfBlob + fetchPayerFeeSchedule | VERIFIED | `getAuthHeaders()` present in all three functions (lines 137, 154, 173) |
| `lib/supabase/middleware.ts` | Defense-in-depth: /api/* auth except /api/public/ and /api/address/ | VERIFIED | No blanket `/api` exclusion; specific public paths listed |
| `components/billing/BillingWorkflow.tsx` | Fee overlay from payer fee schedule | VERIFIED | `payerFeeMap` state initialized, set in 3 places, passed to CptAddDropdown, used in handleAddCpt |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `lib/api-client.ts:fetchPatientInsurance` | `getAuthHeaders()` | Direct call before fetch | WIRED | Line 137: `const headers = await getAuthHeaders()` |
| `lib/supabase/middleware.ts` | `Supabase auth.getUser()` | `!isPublicRoute` gate | WIRED | `/api/public/` and `/api/address/` are the only API exemptions |
| `BillingWorkflow.tsx` | `/api/payers/{payerId}/fee-schedule` | `fetchPayerFeeSchedule` on payer confirm/change | WIRED | Called in handleConfirmPayer (line 345), handlePayerChange (line 360), and mount useEffect (line 309) |
| `BillingWorkflow.tsx:CptAddDropdown` | `feeSchedule` state | `payerFeeMap` prop + fallback | WIRED | Prop passed at line 786; `payerFeeMap?.get(entry.code)` used in dropdown render |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| API-07 | 9.1-01-PLAN, 9.1-02-PLAN | Mock persona seed imports removed from all 9 production pages | SATISFIED | REQUIREMENTS.md marks API-07 as Complete at Phase 9.1; no mock persona imports detected in modified files |

### Anti-Patterns Found

None. No TODO/FIXME/PLACEHOLDER/console.log found in `lib/api-client.ts`. No blanket `/api` exclusion remains in `lib/supabase/middleware.ts`.

### Human Verification Required

#### 1. Payer fee schedule end-to-end in UI

**Test:** Open an encounter in billing, select a payer that has fee schedule overrides in the DB, proceed to the review step, then add a CPT code.
**Expected:** The fee displayed in the CPT dropdown and the new line item fee match the payer-specific rate, not the CPT_CATALOG default.
**Why human:** Requires a real DB with payer fee schedule rows and live UI interaction to confirm the overlay.

#### 2. Self-pay fee behavior

**Test:** Select "Self-pay" as payer, then add a CPT code.
**Expected:** CPT dropdown shows CPT_CATALOG `defaultFee` values; no payer rate applied.
**Why human:** Requires live UI to confirm empty fee map results in correct fallback display.

#### 3. Middleware auth rejection on unauthenticated /api/* request

**Test:** Make an unauthenticated request to `/api/encounters` (or any non-public API route).
**Expected:** Middleware redirects to `/login` (or returns 401).
**Why human:** Requires a live server and browser/curl test without a valid session cookie.

### Gaps Summary

No gaps found. All 8 observable truths are verified against the codebase. Both auth hardening items (api-client JWT headers, middleware blanket exclusion removal) and both fee catalog wiring items (fetchPayerFeeSchedule function, BillingWorkflow fee overlay) are fully implemented and wired.

---

_Verified: 2026-03-26_
_Verifier: Claude (gsd-verifier)_
