---
phase: 11-marketing-pages
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - app/(marketing)/pricing/page.tsx
autonomous: true
requirements:
  - MKT-02
  - MKT-04
  - MKT-05
must_haves:
  truths:
    - "Anonymous visitors to /pricing see exactly three tier cards (Solo, Practice, Scale)"
    - "The Practice tier is visually highlighted ('Most popular' pill or blue border via PricingCard)"
    - "Every tier card has an orange 'Schedule a Demo' CTA with a mailto: href matching tier.ctaHref"
    - "Below the grid: an acknowledgment line 'Pricing finalized Q3 2026 — early adopters lock in launch pricing'"
    - "A 'Why no prices?' explainer renders plain text (no real prices shown anywhere)"
    - "Page exports a Next.js metadata object resolving to 'Pricing | ClarityOS'"
  artifacts:
    - path: "app/(marketing)/pricing/page.tsx"
      provides: "Server-rendered Pricing page composing PRICING_TIERS with PricingCard"
      exports: ["default", "metadata"]
      contains: "PRICING_TIERS"
  key_links:
    - from: "app/(marketing)/pricing/page.tsx"
      to: "app/(marketing)/_data/pricing.ts"
      via: "PRICING_TIERS.map"
      pattern: "PRICING_TIERS"
    - from: "app/(marketing)/pricing/page.tsx"
      to: "app/_components/marketing/PricingCard.tsx"
      via: "default import"
      pattern: "PricingCard"
---

<objective>
Build the public `/pricing` page by composing Wave 0 primitives (`PricingCard`, `SectionHeader`, `CTABanner`) with the typed `PRICING_TIERS` data module. Display 3 placeholder tiers (Solo / Practice / Scale) with orange Schedule-a-Demo CTAs — no real prices, no payment integration.

Purpose: Deliver MKT-02 (3 tiers + orange CTA), MKT-04 (shared chrome), and MKT-05 (per-route metadata). Pure composition page.

Output: `app/(marketing)/pricing/page.tsx` — one server-rendered file.
</objective>

<execution_context>
@C:/Users/duytr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/duytr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-marketing-pages/11-RESEARCH.md
@.planning/phases/11-marketing-pages/11-VALIDATION.md
@.planning/phases/11-marketing-pages/11-01-SUMMARY.md

@app/_components/LandingPage.tsx
@app/(marketing)/layout.tsx
@app/(marketing)/_data/pricing.ts
@app/(marketing)/_data/marketingTokens.ts
@app/_components/marketing/SectionHeader.tsx
@app/_components/marketing/PricingCard.tsx
@app/_components/marketing/CTABanner.tsx
@tests/e2e/marketing-pages.spec.ts

<interfaces>
From `@/app/(marketing)/_data/pricing`:
```ts
export type PricingTier = {
  id: "solo" | "practice" | "scale";
  name: string; tagline: string; icon: LucideIcon;
  priceLabel: string; priceHint: string;
  features: string[];
  ctaLabel: string;  // "Schedule a Demo"
  ctaHref: string;   // mailto:...
  highlight?: boolean;
};
export const PRICING_TIERS: PricingTier[];
```

From `@/app/_components/marketing/PricingCard`:
```tsx
import type { PricingTier } from "@/app/(marketing)/_data/pricing";
export default function PricingCard(props: { tier: PricingTier }): JSX.Element;
```

From `@/app/(marketing)/_data/marketingTokens`:
```ts
export const COLORS = { pageBg, surface, surfaceAlt, border, primary, primaryTint, primarySoft, cta, ctaHover, text, textMuted, textSubtle, success, partial, neutral };
export const FONT_FAMILIES = { heading, body };
```

From `@/app/_components/marketing/SectionHeader` and `CTABanner` — see Plan 02 interfaces block.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Compose the /pricing page</name>
  <files>
    app/(marketing)/pricing/page.tsx
  </files>
  <read_first>
    - app/(marketing)/_data/pricing.ts (PRICING_TIERS shape + 3 tier content)
    - app/_components/marketing/PricingCard.tsx (prop contract: { tier })
    - app/_components/marketing/SectionHeader.tsx
    - app/_components/marketing/CTABanner.tsx
    - app/(marketing)/_data/marketingTokens.ts (COLORS for explainer styling)
    - app/(marketing)/layout.tsx (confirm chrome is already injected)
    - app/_components/LandingPage.tsx (visual tone reference only)
    - .planning/phases/11-marketing-pages/11-RESEARCH.md §Pricing Placeholder Strategy
  </read_first>
  <action>
    Create `app/(marketing)/pricing/page.tsx` as a pure server component. Exact structure:

    ```tsx
    import type { Metadata } from "next";
    import { PRICING_TIERS } from "../_data/pricing";
    import { COLORS, FONT_FAMILIES } from "../_data/marketingTokens";
    import SectionHeader from "@/app/_components/marketing/SectionHeader";
    import PricingCard from "@/app/_components/marketing/PricingCard";
    import CTABanner from "@/app/_components/marketing/CTABanner";

    export const metadata: Metadata = {
      title: "Pricing",
      description:
        "Simple, transparent pricing for solo optometrists to multi-location practices. Schedule a demo for a quote — no hidden fees, no implementation charges.",
      openGraph: {
        title: "ClarityOS Pricing — Simple, Transparent, Optometry-First",
        description: "Three tiers. Book a call and we'll quote you before the end of the conversation.",
        type: "website",
      },
    };

    export default function PricingPage() {
      return (
        <>
          {/* Hero */}
          <section
            style={{
              padding: "7.5rem 2rem 3rem",
              maxWidth: 1160,
              margin: "0 auto",
              textAlign: "center",
            }}
          >
            <SectionHeader
              eyebrow="Pricing"
              title="Simple, transparent pricing"
              subtitle="Three tiers for every practice size. Book a demo and we'll quote you before the call ends."
            />
          </section>

          {/* Tier grid */}
          <section style={{ padding: "1rem 2rem 4rem" }}>
            <div
              style={{
                maxWidth: 1160,
                margin: "0 auto",
                display: "grid",
                gridTemplateColumns: "repeat(auto-fit, minmax(280px, 1fr))",
                gap: "1.5rem",
                alignItems: "stretch",
              }}
            >
              {PRICING_TIERS.map((tier) => (
                <PricingCard key={tier.id} tier={tier} />
              ))}
            </div>
            <p
              style={{
                maxWidth: 1160,
                margin: "2rem auto 0",
                textAlign: "center",
                fontSize: "0.95rem",
                color: COLORS.textMuted,
              }}
            >
              Pricing finalized Q3 2026 — early adopters lock in launch pricing.
            </p>
          </section>

          {/* Why no prices? explainer */}
          <section
            style={{
              padding: "3rem 2rem 4.5rem",
              background: COLORS.surfaceAlt,
            }}
          >
            <div
              style={{
                maxWidth: 760,
                margin: "0 auto",
                background: COLORS.surface,
                border: `1px solid ${COLORS.border}`,
                borderRadius: 12,
                padding: "2rem",
              }}
            >
              <h3
                style={{
                  fontSize: "1.15rem",
                  fontFamily: FONT_FAMILIES.heading,
                  fontWeight: 600,
                  color: COLORS.text,
                  margin: 0,
                  marginBottom: "0.75rem",
                }}
              >
                Why no prices?
              </h3>
              <p
                style={{
                  fontSize: "1rem",
                  color: COLORS.textMuted,
                  lineHeight: 1.6,
                  margin: 0,
                }}
              >
                We&apos;re in pilot. Pricing locks in once we&apos;ve validated with our launch cohort.
                Book a call and we&apos;ll quote you before the end of the conversation — no lead-magnet
                forms, no sales gauntlet.
              </p>
            </div>
          </section>

          {/* Closing CTA */}
          <CTABanner
            title="Ready for a quote?"
            subtitle="20 minutes. We&apos;ll show you the product, quote you a price, and answer every question."
          />
        </>
      );
    }
    ```

    Rules:
    - No `"use client"` directive.
    - Do NOT render MarketingNav/MarketingFooter; do NOT import `mailto` strings directly — each `<PricingCard>` already renders its tier's mailto via the primitive.
    - Do NOT hardcode tier data in JSX — consume `PRICING_TIERS` via `.map`.
    - Do NOT add payment/Stripe logic or any `<form>` elements. (Phase 11 is placeholder-only per research §Deferred Ideas.)
    - If TypeScript warns about unused imports (e.g. FONT_FAMILIES not referenced in final code), trim the import list to match usage.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -15 && grep -c "PRICING_TIERS\|PricingCard" app/\(marketing\)/pricing/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `app/(marketing)/pricing/page.tsx` exists
    - `grep "export const metadata" app/\(marketing\)/pricing/page.tsx` matches
    - `grep 'title: "Pricing"' app/\(marketing\)/pricing/page.tsx` matches
    - `grep "export default function" app/\(marketing\)/pricing/page.tsx` matches
    - `grep "PRICING_TIERS" app/\(marketing\)/pricing/page.tsx` matches (data-driven tiers)
    - `grep "PricingCard" app/\(marketing\)/pricing/page.tsx` matches
    - `grep "Pricing finalized Q3 2026" app/\(marketing\)/pricing/page.tsx` matches (acknowledgment line)
    - `grep "Why no prices?" app/\(marketing\)/pricing/page.tsx` matches (explainer heading)
    - `grep "CTABanner" app/\(marketing\)/pricing/page.tsx` matches
    - `head -3 app/\(marketing\)/pricing/page.tsx` shows no `"use client"` directive
    - `grep "MarketingNav\|MarketingFooter" app/\(marketing\)/pricing/page.tsx` returns no matches
    - `grep "stripe\|<form" app/\(marketing\)/pricing/page.tsx` returns no matches (no deferred features)
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0 and emits `/pricing`
  </acceptance_criteria>
  <done>`/pricing` renders 3 tier cards from `PRICING_TIERS`, highlighted Practice tier visible, acknowledgment copy present, explainer section present, closing CTABanner rendered.</done>
</task>

</tasks>

<verification>
- Route `/pricing` responds 200 in `npm run build` output.
- Playwright test `pricing page shows three tiers and Schedule a Demo CTA` in `tests/e2e/marketing-pages.spec.ts` passes when run headlessly.
- Visual parity with `/features` and LandingPage aesthetic (light bg, #2563EB accents, #F97316 CTA).
</verification>

<success_criteria>
- `npx tsc --noEmit` exits 0
- `npm run build` exits 0 and emits route `/pricing`
- `npx playwright test tests/e2e/marketing-pages.spec.ts -g "pricing"` passes
- Page contains NO real prices (no `$` followed by digits) — `grep -E "\\\$[0-9]" app/\(marketing\)/pricing/page.tsx` returns no matches
- No changes to any Wave 0 artifact or to `app/(marketing)/_data/pricing.ts`
</success_criteria>

<output>
After completion, create `.planning/phases/11-marketing-pages/11-03-SUMMARY.md` capturing:
- Confirm Practice tier rendered as highlighted
- Note any copy deviations from the exact action text (hero subtitle, acknowledgment, explainer)
- Playwright test result: pricing spec pass/fail
</output>
</content>
</invoke>