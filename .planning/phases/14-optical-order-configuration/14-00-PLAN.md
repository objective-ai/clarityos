---
phase: 14-optical-order-configuration
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_optical_order_configuration.py
  - backend/tests/test_optical_suggestions.py
  - backend/tests/test_lens_catalog.py
  - backend/tests/test_job_ticket_pdf.py
  - backend/tests/test_optical_order_contract.py
  - backend/tests/conftest.py
  - tests/contract/lens-catalog.test.ts
  - tests/contract/optical-order-configurator.test.ts
  - tests/contract/order-detail-drawer.test.ts
  - tests/e2e/optical-order-configuration.spec.ts
  - store/__tests__/opticalOrderConfigStore.test.ts
autonomous: true
requirements: [OPT14-01, OPT14-02, OPT14-03, OPT14-04, OPT14-05, OPT14-06, OPT14-07, OPT14-08, OPT14-09, OPT14-10, OPT14-11, OPT14-12, OPT14-13, OPT14-14, OPT14-15, OPT14-16, OPT14-17, OPT14-18]
must_haves:
  truths:
    - "Every Phase 14 task has a runnable pytest/vitest target before implementation begins"
    - "Test files exist as skip-stubs returning pytest.skip until Wave 1+ replaces them"
    - "Wave 0 fixtures cover lens_type_progressive, lens_material_polycarbonate, lens_coating_ar, optical_order_in_draft"
  artifacts:
    - path: "backend/tests/test_optical_order_configuration.py"
      provides: "Skip-stub for configurator PATCH + place validation + suggestions + job ticket"
    - path: "backend/tests/test_optical_suggestions.py"
      provides: "Skip-stub for extract_optical_suggestions()"
    - path: "backend/tests/test_lens_catalog.py"
      provides: "Skip-stub for lens catalog CRUD + partial unique index"
    - path: "backend/tests/test_job_ticket_pdf.py"
      provides: "Skip-stub for reportlab PDF byte assertion"
    - path: "tests/contract/lens-catalog.test.ts"
      provides: "vitest literal-keys assertion stub for LensType/Material/Coating"
    - path: "tests/e2e/optical-order-configuration.spec.ts"
      provides: "Playwright spec skeleton with test.skip()"
  key_links:
    - from: "backend/tests/conftest.py"
      to: "LensType / LensMaterial / LensCoating / OpticalOrder fixtures"
      via: "pytest fixture factories"
      pattern: "lens_type_progressive|lens_material_polycarbonate|optical_order_in_draft"
---

<objective>
Establish Phase 14 test scaffolding before any implementation. Every test file referenced by the Per-Task Verification Map in 14-VALIDATION.md must exist as a skip-stub so Waves 1-4 can replace skip with real assertions task-by-task without "file not found" failures.

Purpose: Per Nyquist Rule (CLAUDE.md/planner role), every `<verify>` block downstream must have a real test path. This plan creates those paths.
Output: 8 backend pytest stubs + 4 frontend vitest/Playwright stubs + fixture extensions in conftest.py.
</objective>

<execution_context>
@C:/Users/duytr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/duytr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-optical-order-configuration/14-CONTEXT.md
@.planning/phases/14-optical-order-configuration/14-RESEARCH.md
@.planning/phases/14-optical-order-configuration/14-VALIDATION.md
@.planning/phases/13-retail-inventory/13-00-SUMMARY.md
@backend/tests/conftest.py
@backend/tests/test_optical_order_contract.py
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend pytest skip-stubs + conftest fixtures</name>
  <read_first>
    - backend/tests/conftest.py (existing Phase 13-00 stub pattern — db_session / tenant_context fixtures)
    - backend/tests/test_optical_order_contract.py (extension target; do not overwrite)
    - .planning/phases/13-retail-inventory/13-00-SUMMARY.md (skip-stub idiom: try/except → pytest.skip(allow_module_level=True))
    - .planning/phases/14-optical-order-configuration/14-RESEARCH.md §"Wave 0 Gaps" (lines 549-559)
  </read_first>
  <files>backend/tests/test_optical_order_configuration.py, backend/tests/test_optical_suggestions.py, backend/tests/test_lens_catalog.py, backend/tests/test_job_ticket_pdf.py, backend/tests/test_optical_order_contract.py, backend/tests/conftest.py</files>
  <action>
    Create 4 new pytest stub files and extend 2 existing files. Use the Phase 13-00 module-level skip idiom (NOT pytest.importorskip — only catches ImportError, not pydantic.ValidationError):

    ```python
    try:
        from backend.services.optical_suggestions import extract_optical_suggestions  # noqa
    except Exception:  # pragma: no cover
        import pytest
        pytest.skip("Phase 14 services not yet implemented", allow_module_level=True)
    ```

    1. **backend/tests/test_optical_order_configuration.py** — Define 6 test functions, all bodied with `pytest.skip("Implemented in Phase 14-04")`:
       - `test_draft_creation_prefills_rx` (OPT14-01)
       - `test_patch_vision_plan_persists` (OPT14-05)
       - `test_patch_rejected_when_status_not_draft` (Pitfall 11)
       - `test_place_validates_seg_height_for_progressive` (OPT14-04 / Pitfall 7)
       - `test_place_validates_vertex_for_requires_vertex_lens` (OPT14-04)
       - `test_place_validation_runs_before_row_lock` (Pitfall 7 — assert no with_for_update fires on 400 path)

    2. **backend/tests/test_optical_suggestions.py** — Define 6 functions, all skipped:
       - `test_extract_progressive_keyword`
       - `test_extract_polycarbonate_for_child`
       - `test_extract_ar_coating`
       - `test_no_ai_data_returns_empty`
       - `test_multiple_coatings_aggregate`
       - `test_walkin_no_encounter_returns_empty` (Pitfall 3)

    3. **backend/tests/test_lens_catalog.py** — Define 5 functions, all skipped:
       - `test_create_lens_type` (OPT14-03)
       - `test_create_lens_material`
       - `test_create_lens_coating`
       - `test_partial_unique_index_allows_inactive_duplicate` (mirrors Phase 13 INV-07)
       - `test_soft_delete_keeps_history` (OPT14-03)

    4. **backend/tests/test_job_ticket_pdf.py** — Define 3 functions, all skipped:
       - `test_job_ticket_pdf_bytes_non_empty` (OPT14-06 — assert >= 1500 bytes)
       - `test_job_ticket_pdf_contains_rx_block` (substring check on PDF text extraction)
       - `test_job_ticket_audit_row_written` (OPT14-10 — JOB_TICKET_GENERATE row in primary TXN)

    5. **backend/tests/test_optical_order_contract.py** — EXTEND existing file. Read first to confirm shape; APPEND new test functions:
       - `test_optical_order_response_contract_phase14_keys` — assert `model_dump(by_alias=True)` keys include `finalRefractionId`, `habitualRefractionId`, `visionPlan`, `fitting`, `jobTicketGeneratedAt`, `suggestionResolutions` (OPT14-17).
       - `test_optical_order_line_item_contract_lens_config` — assert `lensConfig` key present in `OpticalOrderLineItemResponse.model_dump(by_alias=True)`.
       - Both `pytest.skip("Schemas extended in Phase 14-01")` until Wave 1.

    6. **backend/tests/conftest.py** — APPEND fixtures (do NOT modify existing db_session / tenant_context):
       ```python
       @pytest.fixture
       def lens_type_progressive():
           pytest.skip("LensType ORM lands in Phase 14-01")

       @pytest.fixture
       def lens_material_polycarbonate():
           pytest.skip("LensMaterial ORM lands in Phase 14-01")

       @pytest.fixture
       def lens_coating_ar():
           pytest.skip("LensCoating ORM lands in Phase 14-01")

       @pytest.fixture
       def optical_order_in_draft():
           pytest.skip("OpticalOrder factory extension lands in Phase 14-01")
       ```

    Every file must be runnable via `cd backend && pytest tests/test_optical_*.py tests/test_lens_catalog.py tests/test_job_ticket_pdf.py -v` and produce all-SKIPPED output, zero ERRORS.
  </action>
  <verify>
    <automated>cd backend && pytest tests/test_optical_order_configuration.py tests/test_optical_suggestions.py tests/test_lens_catalog.py tests/test_job_ticket_pdf.py tests/test_optical_order_contract.py -v 2>&1 | grep -E "SKIPPED|PASSED|ERROR" | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/tests/test_optical_order_configuration.py` exists; `grep -c "pytest.skip" backend/tests/test_optical_order_configuration.py` returns >= 6.
    - File `backend/tests/test_optical_suggestions.py` exists with 6 skip-stub functions.
    - File `backend/tests/test_lens_catalog.py` exists with 5 skip-stub functions.
    - File `backend/tests/test_job_ticket_pdf.py` exists with 3 skip-stub functions.
    - `grep -c "test_optical_order_response_contract_phase14_keys" backend/tests/test_optical_order_contract.py` returns 1.
    - `grep -c "lens_type_progressive\|lens_material_polycarbonate\|lens_coating_ar\|optical_order_in_draft" backend/tests/conftest.py` returns >= 4.
    - Pytest run shows all-SKIPPED, zero ERRORs (skip-stub idiom working).
  </acceptance_criteria>
  <done>All 5 backend pytest files runnable; conftest fixtures load without error; pytest report shows SKIP count >= 20, ERROR count = 0.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend vitest + Playwright skip-stubs</name>
  <read_first>
    - playwright.config.ts (existing project structure, storageState pattern)
    - tests/e2e/optical-handoff.spec.ts (Phase 6 donor — auth + fixtures pattern; check tests/e2e/*.spec.ts for closest sibling)
    - .planning/phases/14-optical-order-configuration/14-RESEARCH.md §"Wave 0 Gaps" (lines 549-559)
    - .planning/phases/13-retail-inventory/13-14-SUMMARY.md (Phase 13 Playwright optical-order spec — donor for selectors)
    - store/refractionStore.ts (donor for opticalOrderConfigStore test shape)
  </read_first>
  <files>tests/contract/lens-catalog.test.ts, tests/contract/optical-order-configurator.test.ts, tests/contract/order-detail-drawer.test.ts, tests/e2e/optical-order-configuration.spec.ts, store/__tests__/opticalOrderConfigStore.test.ts</files>
  <action>
    Create 5 frontend test skip-stubs. Use `it.skip()` / `test.skip()` so files compile cleanly under tsc and produce 0 failures:

    1. **tests/contract/lens-catalog.test.ts** — vitest literal-keys assertion stub. Shape:
       ```ts
       import { describe, it, expect } from "vitest";
       describe("LensType contract", () => {
         it.skip("matches backend by_alias snapshot", () => {
           const expectedKeys = ["id", "tenantId", "name", "requiresSegHeight", "requiresVertex", "displayOrder", "isActive", "createdAt", "updatedAt"];
           expect(expectedKeys).toBeDefined();
         });
       });
       describe("LensMaterial contract", () => {
         it.skip("matches backend by_alias snapshot", () => {});
       });
       describe("LensCoating contract", () => {
         it.skip("matches backend by_alias snapshot", () => {});
       });
       ```

    2. **tests/contract/optical-order-configurator.test.ts** — vitest stub asserting OpticalOrderResponse Phase 14 key extensions (`finalRefractionId`, `habitualRefractionId`, `visionPlan`, `fitting`, `jobTicketGeneratedAt`, `suggestionResolutions`). 1 it.skip per key (6 total).

    3. **tests/contract/order-detail-drawer.test.ts** — vitest stub for OrderDetailDrawer prop shape + routing logic (draft → configurator, non-draft → drawer). 3 it.skip blocks: `routes_draft_to_configurator`, `opens_drawer_for_placed`, `cancel_cta_gated_on_permission`.

    4. **tests/e2e/optical-order-configuration.spec.ts** — Playwright test.skip stubs covering OPT14-18 master flow:
       ```ts
       import { test, expect } from "../fixtures";
       test.describe("Optical Order Configuration E2E", () => {
         test.skip("queue → Configure Order CTA → opens configurator with Final Rx prefilled", async () => {});
         test.skip("autosave triggers PATCH after 1.5s + on blur", async () => {});
         test.skip("place fails with field_errors when seg_height missing for progressive", async () => {});
         test.skip("place succeeds → Generate Job Ticket downloads PDF", async () => {});
         test.skip("draft-pending pill renders on queue card and routes to configurator", async () => {});
         test.skip("non-draft order opens OrderDetailDrawer instead of configurator", async () => {});
       });
       ```
       Use the existing `./fixtures` import shape from `.claude/rules/testing.md`.

    5. **store/__tests__/opticalOrderConfigStore.test.ts** — vitest fake-timer stub for 1.5s debounce + flush-on-blur (OPT14-12):
       ```ts
       import { describe, it, vi } from "vitest";
       describe("opticalOrderConfigStore", () => {
         it.skip("debounces patch by 1.5s", () => { vi.useFakeTimers(); });
         it.skip("flushes pending patches on blur", () => {});
         it.skip("no-ops when status != 'draft'", () => {}); // Pitfall 11
       });
       ```

    All files must pass `npx tsc --noEmit` and `npx vitest run` with 0 failures (skipped tests count as passing).
  </action>
  <verify>
    <automated>npx vitest run tests/contract/lens-catalog.test.ts tests/contract/optical-order-configurator.test.ts tests/contract/order-detail-drawer.test.ts store/__tests__/opticalOrderConfigStore.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "it.skip\|test.skip" tests/contract/lens-catalog.test.ts` returns >= 3.
    - `grep -c "it.skip" tests/contract/optical-order-configurator.test.ts` returns >= 6.
    - `grep -c "it.skip" tests/contract/order-detail-drawer.test.ts` returns >= 3.
    - `grep -c "test.skip" tests/e2e/optical-order-configuration.spec.ts` returns >= 6.
    - `grep -c "it.skip" store/__tests__/opticalOrderConfigStore.test.ts` returns >= 3.
    - `npx tsc --noEmit` exits 0 (no TS errors introduced).
    - `npx vitest run tests/contract/lens-catalog.test.ts` reports 0 failures.
  </acceptance_criteria>
  <done>All 5 frontend test stubs compile under tsc, vitest reports 0 failures, Playwright spec at least lists 6 skipped tests.</done>
</task>

<task type="auto">
  <name>Task 3: Append OPT14-01..OPT14-18 to REQUIREMENTS.md + Traceability rows</name>
  <read_first>
    - .planning/REQUIREMENTS.md (existing — see Phase 13 INV section lines 140-161 for format; Traceability table at line 211+)
    - .planning/phases/14-optical-order-configuration/14-RESEARCH.md §Phase Requirements (lines 63-89; OPT14-01..OPT14-18 with descriptions + Research Support column)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    APPEND a new section after "### Retail Inventory (Phase 13)" (after line 161) and before "## v2 Requirements":

    ```
    ### Optical Order Configuration (Phase 14)

    - [ ] **OPT14-01**: Configurator route opens from optical queue with Final Rx pre-populated and PD pre-filled from `refraction.pd_distance` / `pd_near` (ROADMAP success criterion #1)
    - [ ] **OPT14-02**: Habitual Rx column rendered side-by-side with Final Rx in configurator left pane (OD/OS × sphere/cyl/axis/add/prism); delta-flag row when |Final − Habitual| SE > 0.50D (ROADMAP success criterion #2)
    - [ ] **OPT14-03**: Frame picker reads Phase 13 products?type=frame catalog; lens type/material/coatings selectable from new admin-managed reference catalogs `lens_types`/`lens_materials`/`lens_coatings` (ROADMAP success criterion #3)
    - [ ] **OPT14-04**: Seg height OD/OS captured; required-marker triggers when `lens_type.requires_seg_height=true`; vertex distance required when `requires_vertex=true`; place handler 400s with `field_errors: [{path, code, message}]` on missing required fields (ROADMAP success criterion #4)
    - [ ] **OPT14-05**: Vision plan name, member ID, group number captured in `vision_plan_jsonb` (snake_case keys: name, member_id, group_number, authorization_number?, copay?, allowance?) (ROADMAP success criterion #5)
    - [ ] **OPT14-06**: `POST /optical-orders/{id}/job-ticket/` returns reportlab PDF with header (clinic), patient block, two-column Rx (Habitual | Final), frame, lens, coatings, measurements, vision plan, footer (staff + timestamp); sets `job_ticket_generated_at`; gated on `status='placed'` (ROADMAP success criterion #6)
    - [ ] **OPT14-07**: AI Scribe optical recommendations surface as ghosted ✨ chips inline in configurator; accept fills field; dismiss persists to `OpticalOrder.suggestion_resolutions_jsonb`; deterministic keyword extractor — no new Claude calls (ROADMAP success criterion #7)
    - [ ] **OPT14-08**: Alembic migration 0019 adds 3 lens reference tables (`lens_types`, `lens_materials`, `lens_coatings`) + 5 OpticalOrder columns (`vision_plan_jsonb`, `fitting_jsonb`, `suggestion_resolutions_jsonb`, `final_refraction_id`, `habitual_refraction_id`, `job_ticket_generated_at`) + 1 OpticalOrderLineItem column (`lens_config_jsonb`); JSONB server_default via `sa.text("'{}'::jsonb")`; partial unique indexes `(tenant_id, name) WHERE is_active=true` on each reference table
    - [ ] **OPT14-09**: 2 new ClinicalAction values wired into PERMISSION_MATRIX: `GENERATE_JOB_TICKET` {T,R,A,O}, `MANAGE_LENS_CATALOG` {A,O}
    - [ ] **OPT14-10**: 5 new AuditAction VARCHAR values logged via `log_action()` in primary TXN: `OPTICAL_ORDER_CONFIGURE_UPDATE`, `JOB_TICKET_GENERATE`, `LENS_TYPE_CREATE`, `LENS_MATERIAL_CREATE`, `LENS_COATING_CREATE`; UPDATE/DEACTIVATE reuse with `metadata.action` discriminator
    - [ ] **OPT14-11**: `_seed_lens_reference()` idempotently seeds 4 lens types (Single Vision, Bifocal, Progressive, Reading) + 6 materials (CR-39, polycarbonate, trivex, hi-index 1.67/1.74/1.80) + 7 coatings (AR, UV, blue light, photochromic, polarized, scratch-resistant, mirror); wired into `seed_tenant_schema()` after `_seed_retail_inventory()`
    - [ ] **OPT14-12**: Full-page configurator route at `app/(tenant)/[tenant]/optical/orders/[orderId]/page.tsx`; autosave 1.5s debounce + flush-on-blur via `opticalOrderConfigStore` (raw fetch + getAuthHeaders to preserve JSONB snake_case per Pitfall 1); no-ops when status != 'draft' (Pitfall 11)
    - [ ] **OPT14-13**: Three configurator entry points wired: (1) optical-queue card "Configure Order" CTA, (2) Patient Orders tab draft-order click, (3) walk-in modal redirect on submit for spectacle orders
    - [ ] **OPT14-14**: `OpticalQueueItem.draft_order_count: int = 0` schema field + "Draft pending" pill on `OpticalQueueCard.tsx` (absorbs 2026-05-08-optical-queue-draft-order-indicator todo); pill click routes to most recent draft (Open Question #3 resolution)
    - [ ] **OPT14-15**: `OrderDetailDrawer` component — 480px right-slide drawer with ESC + backdrop close, hydration safety (`if (!open && !order) return null`), renders status timeline + line items + lens config (read-only) + vision plan + Generate Job Ticket button; mirrors `AppointmentDetailDrawer.tsx`; absorbs Phase 13 INV-15
    - [ ] **OPT14-16**: BFF proxies registered for all new endpoints (11 routes): PATCH /optical-orders/[id]/; POST /optical-orders/[id]/job-ticket/ (raw fetch — Blob); GET /optical-orders/[id]/suggestions/; POST /optical-orders/[id]/suggestions/[suggestionId]/{accept|dismiss}/; lens-catalog/{types|materials|coatings}/ GET+POST + [id]/ GET+PATCH+DELETE; all with trailing-slash upstream URLs
    - [ ] **OPT14-17**: Pydantic `by_alias` contract tests for `OpticalOrderResponse` (extended), `OpticalOrderLineItemResponse` (lens_config), `LensTypeResponse`, `LensMaterialResponse`, `LensCoatingResponse`, `JobTicketMetaResponse`; vitest literal-keys mirror per `feedback_contract_tests.md`
    - [ ] **OPT14-18**: Playwright E2E covering optical queue → "Configure Order" CTA → configurator autosave (PATCH intercept) → place with missing seg height (400 field_errors) → fix → place succeeds → Generate Job Ticket (PDF download) → OrderDetailDrawer view for placed order
    ```

    Then APPEND to the Traceability table (after the last INV-20 row):
    ```
    | OPT14-01 | Phase 14 | Pending |
    | OPT14-02 | Phase 14 | Pending |
    | OPT14-03 | Phase 14 | Pending |
    | OPT14-04 | Phase 14 | Pending |
    | OPT14-05 | Phase 14 | Pending |
    | OPT14-06 | Phase 14 | Pending |
    | OPT14-07 | Phase 14 | Pending |
    | OPT14-08 | Phase 14 | Pending |
    | OPT14-09 | Phase 14 | Pending |
    | OPT14-10 | Phase 14 | Pending |
    | OPT14-11 | Phase 14 | Pending |
    | OPT14-12 | Phase 14 | Pending |
    | OPT14-13 | Phase 14 | Pending |
    | OPT14-14 | Phase 14 | Pending |
    | OPT14-15 | Phase 14 | Pending |
    | OPT14-16 | Phase 14 | Pending |
    | OPT14-17 | Phase 14 | Pending |
    | OPT14-18 | Phase 14 | Pending |
    ```

    Update the Coverage block at file bottom: Total requirements 123 → 141; Pending 56 → 74. Update "Last updated" line to 2026-05-14 with note "Phase 14 Optical Order Configuration (18 OPT14 requirements added)".
  </action>
  <verify>
    <automated>grep -c "^- \[ \] \*\*OPT14-" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[ \] \*\*OPT14-" .planning/REQUIREMENTS.md` returns 18.
    - `grep -c "| OPT14-.. | Phase 14 | Pending |" .planning/REQUIREMENTS.md` returns 18.
    - `grep -c "### Optical Order Configuration (Phase 14)" .planning/REQUIREMENTS.md` returns 1.
    - Coverage block updated: `grep "Total requirements: 141" .planning/REQUIREMENTS.md` returns 1 line.
    - Last updated line mentions "2026-05-14" and "Phase 14".
  </acceptance_criteria>
  <done>18 OPT14 requirement rows present in section header, 18 matching Traceability rows present, coverage counts updated.</done>
</task>

</tasks>

<verification>
- Backend: `cd backend && pytest tests/test_optical_order_configuration.py tests/test_optical_suggestions.py tests/test_lens_catalog.py tests/test_job_ticket_pdf.py tests/test_optical_order_contract.py -v` shows >= 20 SKIPPED, 0 ERRORS.
- Frontend: `npx vitest run tests/contract/ store/__tests__/opticalOrderConfigStore.test.ts` reports 0 failures.
- `npx tsc --noEmit` exits 0.
- `grep -c "^- \[ \] \*\*OPT14-" .planning/REQUIREMENTS.md` returns 18.
</verification>

<success_criteria>
- All Phase 14 downstream plans have a runnable test path their `<verify>` blocks reference.
- REQUIREMENTS.md has Phase 14 section + 18 OPT14 rows + 18 Traceability rows.
- conftest.py exports 4 new fixtures as skip-stubs.
- Zero implementation code introduced — only test scaffolding + requirements text.
</success_criteria>

<output>
After completion, create `.planning/phases/14-optical-order-configuration/14-00-SUMMARY.md` per the standard template.
</output>
