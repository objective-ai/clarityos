import { test } from './fixtures';

// Wave-0 skeleton. Plan 15-11 activates these specs after Plan 15-05 ships
// the refund lifecycle and Plan 15-10 wires the RefundDialog UI.
test.skip('POS-05 — item-level refund restocks product line; superbill line does not restock', async () => {
  // Activates in Plan 15-11
});

test.skip('POS-09 — all-lines-refunded cascades OpticalOrder.status to cancelled', async () => {
  // Activates in Plan 15-11
});
