import { test } from './fixtures';

// Wave-0 skeleton. Plan 15-11 activates these specs after Plan 15-09 ships
// the /pos page and Plan 15-08 wires the BFF routes.
test.skip('POS-01 — cart loads from superbill + optical order + ad-hoc product', async () => {
  // Activates in Plan 15-11
});

test.skip('POS-02 — cash payment + change_due + close sale', async () => {
  // Activates in Plan 15-11
});

test.skip('POS-02 — Stripe Elements card payment + server-confirm + close sale', async () => {
  // Activates in Plan 15-11
});
