import { test } from './fixtures';

// Wave-0 skeleton. Plan 15-11 activates these specs after Plan 15-07 ships
// the daily-close service and Plan 15-10 wires the close-of-day page.
test.skip('POS-04 — daily close totals by method + category with PDF + CSV export', async () => {
  // Activates in Plan 15-11
});

test.skip('POS-10 — cash reconciliation persists expected/counted/variance per close_date', async () => {
  // Activates in Plan 15-11
});
